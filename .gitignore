node_modules
.next
out
npm-debug.log*

# Slidev build output — regenerated on each Pages deploy via `npm run prebuild`
public/slides/

# Per-lesson reference solutions, bundled into a server-only map by
# scripts/generate-solutions.mjs during prebuild.
functions/_shared/solutions.generated.ts

# Per-lesson starter file trees, bundled into a server-only map by
# scripts/generate-lesson-starters.mjs during prebuild.
functions/_shared/lesson-starters.generated.ts

# TypeScript incremental build info
tsconfig.tsbuildinfo

# Editor / tool scratch
.claude/
.playwright-mcp/
tmp/

# Wrangler local dev (contains AUTH_SECRET, D1 state, etc.)
.dev.vars
.wrangler/

# Local reference screenshots
unit-5-1-ordered.png
home-unauth.png
lesson-footer.png
module-2.1-footer.png
module-2.1-unauth.png
graphify-out/

# cross-CLI message log (Claude Code <-> opencode), local only
.msgbox/
