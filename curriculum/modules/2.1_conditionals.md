---
id: "2.1"
title: Conditionals
quarter: 1
weeks: [5]
environment: console
status: draft
---

# Module 2.1 — Conditionals

**Quarter 1 · Week 5 · Environment: browser DevTools console**
**Meets Tue Sep 08 + Thu Sep 10, 2026 (2 meetings, book §2.1 Conditionals, 1110 lines — largest section in the book).**

## SLO Focus
- Reinforces **SLO 3** — students implement their first structured programs with branching.

## Module Goal
Students can make decisions in code: `if` / `else if` / `else`, truthy/falsy conversion, the conditional (ternary) operator `?`, comparison operators (`===` vs `==`), and logical operators (`&& || !`).

## Pre-requisite snapshot
Unit 1 complete — students can declare variables, use the four primitive types, and write expressions.

## Post-requisite snapshot
Students can branch with `if`/`else if`/`else`, pick values with `?`, and combine conditions with logical operators. Ready for §2.2 Algorithms and Loops.

## Lesson count
**40 lessons** (`2.1.1`–`2.1.40`), up from 14. 26 new lessons added in the 2026-08-16 buildout — see Note below.

## Assignments
- **A2.1.1** (`2.1.39`) — Lab: grade recommendation advisor using `score`/`attendance`/`lateAssignments`, an `else if` chain, and at least one `&&`/`||`.
- **A2.1.2** (`2.1.40`, last lesson in the section) — Quiz: `===` vs `==`, operator precedence, and tracing three conditional snippets. AI-graded written assignment.

## Note
Split out of the former combined "1.2 Control Flow" unit spec during the
2026-08-12 book-native renumber (book §2.1 Conditionals).

**2026-08-16 buildout:** expanded from 14 lessons to 40 against a full read of
book §2.1 (Pass A: `if`/truthy-falsy/`else`/`else if`/ternary/chained
ternary/ternary-vs-if/`&&`/`||`/`!`, all eight book subsections plus both
Definitions) and the curriculum plan's named in-class activities (Pass B:
nested conditionals, the "print settings advisor" scenario). `week` corrected
from the stale `4` to `5` (curriculum-plan schedules §2.1 at calendar week 5,
Tue Sep 08 + Thu Sep 10, 2026) across every lesson in the module, including
the 14 pre-existing ones. Fixed a real defect found in the pre-existing
`2-1-4a-chart-the-if-else`: its title carried a retired letter suffix
(`2.1.5a`), which the strict `\d+\.\d+\.\d+` title parser silently drops from
`/module/2.1` — renumbered to the integer-only `2.1.5`. Every lesson,
including the 14 carried over, now has a `sourceRef` tagging it Pass A
(book-sourced) or Pass B (designed scaffolding). Book Problem Set items were
read for intent and folded into designed lab/quiz scenarios rather than
transcribed 1:1, per `BOOK-TO-MODULE.md` §5 ("shCode invents its own graded
work... do not port the book's Problem Sets into lessons as assignments").
