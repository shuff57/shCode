---
id: "2.2"
title: Algorithms and Loops
quarter: 1
weeks: [6]
environment: console
status: built
---

# Module 2.2: Algorithms and Loops

**Quarter 1 · Week 6 · Meets Mon Sep 14, 2026 · Environment: browser DevTools console**

## SLO Focus
- **Introduces SLO 4** (algorithms): primary artifact **A2.2.1** (Written + Lab), gated by the **A2.2.0** flowchart.
- Reinforces SLO 3 (`for`, `while`, `do…while`).

## Module Goal
Students define what an algorithm is, see that a program is an algorithm written
in a language a computer can run, and repeat work with loops: `for`, `while`,
and `do…while`. They trace loops to predict output, avoid infinite loops, use
a step other than 1, and meet the idea of a canonical algorithm (linear search)
without yet having arrays or `break` to write the full version.

22 lessons: 16 shipped in the 2026-08-15 diagram-native pilot, 4 added in the
2026-08-16 coverage pass to close book-coverage gaps (§Note below), and 2 more
(`2.2.19`/`2.2.20`, Example 2.2.6 Walking Through a Word) added afterwards.

## Pre-requisite snapshot
Module 2.1 (Conditionals) complete.

## Post-requisite snapshot
Students can trace and write `for`/`while`/`do…while` loops, state precisely
what an algorithm is and how a program differs from one, use a `for` loop with
a step other than 1, and describe what a canonical algorithm is.

## Book coverage (built §2.2, checked 2026-09-05)

Every subsection of the built book section has at least one lesson.

| Book §2.2 subsection | Lessons |
| --- | --- |
| 2.2.1 What Is an Algorithm? | 2.2.2, 2.2.5, 2.2.8 |
| 2.2.2 Programs Implement Algorithms | 2.2.3 |
| 2.2.3 Common Ideas Across Languages | 2.2.15 |
| 2.2.4 Introduction to Loops | 2.2.9, 2.2.10, 2.2.13, 2.2.14, 2.2.19, 2.2.20 |
| 2.2.5 Canonical Algorithms | 2.2.21 |

shCode-designed, anchored to no single subsection: `2.2.1` (deck), `2.2.4`, `2.2.6`,
`2.2.11`, `2.2.18` (the Appendix D diagram thread), `2.2.7` (**A2.2.0** gate),
`2.2.12`, `2.2.16` (Problem Set practice), `2.2.17` (**A2.2.1** Part B), `2.2.22`
(challenges).

**Resolved 2026-09-05 — `do...while` removed from this module.** Book §2.2 contains
zero occurrences of `do...while` (measured across the section *and* its solutions
file); the book introduces it in §2.4.2, which module 2.4 covers at `2.4.5`-`2.4.8`.
Lessons `2.2.13` and `2.2.14` taught it two modules early, and 2.4 then re-taught it.
The `do...while` half of both lessons is now gone: `2.2.13` is retitled
"Reading: The while Loop" and `2.2.14` drops its Step 2, so this module teaches `for`
and `while` only, exactly as the book does.

**Directive: do not re-add `do...while` to module 2.2.** `2.4.5` opens with "Both
loops you've met so far: `for` and `while`" -- a sentence that was false while the
preview existed and is true now. Anything reintroduced here breaks it silently, and
nothing in `npm test` reads book prose to catch that.

Neither folder was renamed and neither lesson number moved, so student progress on
`2-2-8-reading-while-loop` and `2-2-9-example-while-vs-for` is untouched.

## Note
Split out of the former combined "1.2 Control Flow" unit spec during the
2026-08-12 book-native renumber (book §2.2 Algorithms and Loops).

**2026-08-16 coverage pass** (this build): verified all 16 existing lessons
against the book, renumbered the A2.2.0 gate chart (`2-2-12-a5-2-flowchart-decision`)
from 2.2.15 to 2.2.7 so it actually precedes the artifact it gates, added
"A2.2.0"/"A2.2.1 (Part A)" codes to the two existing assignment titles, and
added 4 lessons to close gaps the coverage table found with no lesson at all:
2.2.3 Reading: Programs Implement Algorithms (book §2.2.2, previously
uncovered), 2.2.15 Worked Example: Same Algorithm, Three Spellings (book
§2.2.3, previously uncovered), 2.2.17 A2.2.1 (Part B), For + While Loop
Program (curriculum-plan.md's A2.2.1 had no lab half built), and 2.2.19
Reading: Canonical Algorithms & Linear Search (book §2.2.5, previously
uncovered: searches a number range instead of an array, since arrays/`break`
aren't taught until 3.3/2.4). Also fixed: an existing challenge step that
required arrays before they're taught, a stale numbering leftover from the
pre-renumber scheme in two labs' scaffold files, and a missing `for`-loop
step-size example (book Example 2.4 / Try It Now 2.4).

**2026-08-16, later same day:** the first pass of this note used letter-suffixed
title numbers (`2.2.2a`, `2.2.5b`, etc.) to insert lessons without renumbering
their neighbors. That convention is broken in this codebase: `displayTitle`
(`lib/curriculum.ts` line 136) strips only the leading `\d+\.\d+\.\d+`, so the
suffix letter leaked into the rendered title, and `numberedId` also drops the
suffix, so two lessons could parse to the same id and sort by directory-read
order instead of the authored order: silently undoing the gate-before-artifact
fix. All lessons are now renumbered to plain contiguous integers, title-only; no
folder was renamed. The module currently runs 2.2.1–2.2.22. Full render order and the corrected
verification output are in the build report.
