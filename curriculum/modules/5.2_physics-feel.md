---
id: "5.2"
title: Physics Feel
unit: "5.2 Physics Feel"
quarter: 2
weeks: [15]
bookSection: "5.2"
lessonSlots: "5.2.1–5.2.15"
environment: moSHion
slos:
  primary: [SLO-3]
artifacts:
  - { id: A5.2.1, type: lab, slo: SLO-3, role: primary }
  - { id: A5.2.2, type: written, slo: SLO-3, role: support }
prerequisites:
  - "5.1 Hello Sprite and Movement complete: Canvas, Sprite, kb.pressing(), vel.x/y, the frame loop"
externalSources:
  moshionDocsInApp: "/docs/moshion"
status: shipped
---

# Module 5.2: Physics Feel

**Quarter 2 · ~1.75 contact hours (1 class meeting)**
**Environment:** moSHion in-app editor (built on q5.js + Box2D physics).

## Context

§5.1 gave students a sprite that moves because they set `.vel` directly, every frame,
themselves. §5.2 hands the same scene to a physics engine and asks students to tune it
instead of drive it: gravity, bounciness, friction, and `applyForce` are all dials, and
—per the book's own Context Pause—the section's real subject is what turning each dial
does to how the game *feels*, not a single correct value. That framing should carry into
every lesson here: labs ask students to try several values and describe the difference,
not to hit one target number.

The section also does double duty as the last stop before OOP (§5.3 next): the finished
player/ground/goal/wall scene built lesson-by-lesson through §5.2.1–§5.2.5 is exactly the
program §5.4 later refactors into a `Goal` class. Nothing here should pre-empt that
refactor — the win condition stays a loose `goal` variable and a manual distance check,
even though it's clunky, because the clunkiness is what makes §5.4's class version land.

**Only one lesson currently exists for this module**, `moshion-bounce` (title `5.2.1
Bouncy Ball`), and it was orphaned until 2026-08-16: no `id: "5.2"` module file existed,
so it rendered on no `/module` page and was reachable only by direct URL. This spec
folds it back in as a carry-over (§ In-App Lessons below) and designs the other 14
lesson slots around it.

**Do NOT:**
- Explain `kinematic` bodies. The book never defines the term (only `dynamic`, the
  default, and `static`, used for `ground`/walls) — teaching a third body type here adds
  vocabulary the book and the graded work never test. Treat "dynamic vs static" as the
  actual pair; if `kinematic` comes up, it's a vocabulary aside, not a lesson slot.
- Let a lab reward one "correct" gravity/bounciness/friction value. The grader can check
  that a property was *set*, never that it was set to the *right* feeling — that judgment
  is what the written assignment (A5.2.2) and in-class discussion are for.
- Pre-empt §5.4 by wrapping the goal in a class, a function that returns early, or any
  other cleanup. The manual, slightly repetitive version is the point.

## Learning Objectives

Students will be able to:
1. Set `world.gravity.y` and predict its effect (falls, floats, drifts up) on a dynamic sprite.
2. Distinguish a `dynamic` sprite (default; affected by gravity/forces) from a `static` one (immovable; used for ground/walls).
3. Gate an action on a physics condition using `sprite.colliding(otherSprite)`.
4. Explain why `kb.presses(...)` (edge-triggered) is the right choice for a jump, where `kb.pressing(...)` (level-triggered, from §5.1) is not.
5. Set `.bounciness` and `.friction` on a sprite and predict the qualitative effect of low vs. high values.
6. State the two-sprite combining rule: bounciness uses the higher value, friction uses a geometric mean.
7. Call `sprite.applyForce(fx, fy)` and explain how it differs from setting `.vel` directly (units, mass, additive vs. overriding).
8. Call `sprite.delete()` and explain why it's different from `sprite.visible = false`.
9. Write a single-fire event check (a condition guarded so its side effect happens exactly once, not every frame it stays true).

## Topics Covered

*Gravity*
- `world.gravity.y`: sets vertical gravity for every dynamic sprite; positive down, `0` off, negative up
- `dynamic` vs `static` sprite bodies

*Ground-gated actions*
- `sprite.colliding(otherSprite)`: true while two sprites touch
- `kb.presses(key)` (edge-triggered, fires once per press) vs `kb.pressing(key)` (level-triggered, §5.1)
- Combining a key-press with a ground check to gate a jump

*Bounciness and friction*
- `.bounciness` (restitution): 0 sticks, 1 bounces at full speed
- `.friction`: 0 is ice, higher drags motion to a stop
- Two-sprite combining rule: bounciness uses the max, friction uses a geometric mean

*Forces*
- `sprite.applyForce(fx, fy)`: Newtons, respects mass, adds to existing motion
- `.vel` vs `applyForce`: overriding motion vs. nudging it

*Cleanup and win conditions*
- `sprite.delete()`: permanent, no undo, vs. `.visible = false` (temporary hide)
- Single-fire pattern: guard a repeatable condition so its side effect (a `console.log`, a state change) happens once

## Prerequisites

- Module 5.1 complete: `Canvas`, `Sprite(x, y, w, h)`, `.color`, `kb.pressing(...)`, `.vel.x/y`, the frame loop.
- Comfortable with `if`/`else if`/`else` (Q1).

## Environment Setup

Standard moSHion setup. No handouts or extra tools beyond the in-app editor.

## Videos

| # | Source | Title | Description hook | YouTube URL |
|---|---|---|---|---|
| — | — | — | none planned for this module | |

## Readings

| # | Source | Section | Title | Notes |
|---|---|---|---|---|
| R1 | Internal | book §5.2.1 | Gravity | `world.gravity.y`, dynamic vs static |
| R2 | Internal | book §5.2.2 | Ground-Gated Actions | `colliding()`, `kb.presses` vs `kb.pressing` |
| R3 | Internal | book §5.2.3 | Bounciness and Friction | the combining rule |
| R4 | Internal | book §5.2.4 | Forces vs Velocity | `applyForce`, Newtons, mass |
| R5 | Internal | book §5.2.5 | Cleanup and Delete | `delete()` vs `.visible` |

### Reading content guidance

#### R1: Gravity (`5-2-2-reading-gravity`)

- **Topic 1: `world.gravity.y` and dynamic vs static**: read before `5.2.3 Lab: Tune Gravity`.
  - **What you'll learn from it**: gravity is a per-world setting, not per-sprite; positive/zero/negative values; why `ground` is `static` and `player` isn't.
  - **Try it:** set `world.gravity.y` to `10`, `0`, `-5` in a live block with one dynamic sprite and log its `.y` each second; watch it fall, float, or drift up.
  - **Glossary rows:** `Gravity`, `dynamic`, `static`.

#### R2: Ground-Gated Actions (`5-2-4-reading-ground-check`)

- **Topic 1: `colliding()` and edge-triggered input**: read before `5.2.5 Lab: Add a Jump`.
  - **What you'll learn from it**: `sprite.colliding(other)` returns true while touching; `kb.presses` fires once per press where `kb.pressing` (§5.1) fires every frame held; why a jump needs both a press check and a ground check together.
  - **Try it:** live block that logs `"touching"` every frame `player.colliding(ground)` is true, then a second block that logs a message once per space-bar press using `kb.presses(' ')`.
  - **Glossary rows:** `colliding()`, `kb.presses` (edge-triggered).

#### R3: Bounciness and Friction (`5-2-6-reading-bounciness-friction`)

- **Topic 1: the two properties and the combining rule**: read before `5.2.7 Bouncy Ball`.
  - **What you'll learn from it**: bounciness (0–1) is retained speed after a collision; friction resists sliding; when two sprites' values disagree, bounciness takes the higher, friction takes a geometric mean.
  - **Try it:** set `.bounciness = 0.8` on a dropped sprite over a `static` floor and watch it rebound; then set it to `0` and compare.
  - **Glossary rows:** `Bounciness`, `Friction`.

#### R4: Forces vs Velocity (`5-2-8-reading-forces-vs-velocity`)

- **Topic 1: `applyForce` vs setting `.vel`**: read before `5.2.9 Lab: Wind Zone`.
  - **What you'll learn from it**: `applyForce(fx, fy)` is measured in Newtons, respects mass, and adds to existing motion; setting `.vel` directly (§5.1's idiom) overrides motion outright; when each is the right tool.
  - **Try it:** call `player.applyForce(0, -3)` every frame while a key is held, layered on top of gravity, and watch the sprite's fall slow rather than stop outright.
  - **Glossary rows:** `applyForce`.

#### R5: Cleanup and Delete (`5-2-10-reading-delete`)

- **Topic 1: `delete()` vs `.visible`**: read before `5.2.11 Lab: Reaching the Goal`.
  - **What you'll learn from it**: `sprite.delete()` permanently removes a sprite (drawing, updating, and its physics body all gone, no undo); `.visible = false` only hides it, everything else keeps running.
  - **Try it:** delete a sprite on a key press and confirm a second press does nothing (it's really gone), versus toggling `.visible` and confirming the sprite comes back.
  - **Glossary rows:** `delete()`.

## In-App Lessons (carry-overs)

### `lessons/moshion-bounce/` → stays at `lessons/moshion-bounce/`, retitled to slot 5.2.7

- **New title:** `5.2.7 Bouncy Ball` (was already `5.2.1 Bouncy Ball` — renumbered to make
  room for the atomic gravity/jump lessons ahead of it in this spec; title rename only,
  folder stays put so existing student progress survives).
- **Convention violations to fix:**
  - `grading.totalPoints: 30`, `passingScore: 20`, and four non-zero `requirements[].points`
    → all to `0` per `moshion-lesson-conventions.md` (mastery, all-green Submit gate).
  - `category: "Unit 2: moSHion: Applied Game Development"` → `"Unit 5: moSHion Foundations"`
  - `week: 14` → `15` (this module's actual schedule slot, see frontmatter)
  - `slos: ["SLO-1"]` → `["SLO-3"]` (matches this module's SLO focus; `SLO-1` was miscoded)
- **Content is otherwise correctly scoped** (walls, a dynamic circle sprite, bounciness,
  friction) and can stand as the module's bounciness/friction integration checkpoint —
  no need to also author a separate `5.2.7 Lab: Bounce and Slide` from scratch.
- **Data continuity:** folder name unchanged, so no student progress is at risk.

## Worked Examples (teacher-led)

### Worked Example 1: The Finished Scene (10 min)

Live-code (or walk through) the fully assembled player/ground/goal/walls scene from
book §5.2.5's solution: gravity, a ground-gated jump, bounciness/friction on player and
ground, a wind-zone force above y=200, and a single-fire "You win!" when the player
reaches the goal. This is the same scene A5.2.1 asks students to adapt into a pinball
layout — walking through the complete version first means the graded lab is a
modification, not a blank page.

```js
let player, ground, goal, leftWall, rightWall;

function setup() {
  new Canvas(400, 400);
  world.gravity.y = 10;
  player = new Sprite(200, 60, 40, 40);
  player.color = 'deepskyblue';
  player.bounciness = 0.6;

  ground = new Sprite(200, 380, 400, 20, 'static');
  ground.friction = 0.5;

  goal = new Sprite(360, 340, 24, 24);
  goal.collider = 'none';
  goal.color = 'gold';

  leftWall = new Sprite(6, 200, 12, 400, 'static');
  leftWall.color = 'sienna';
  rightWall = new Sprite(394, 200, 12, 400, 'static');
  rightWall.color = 'sienna';
}

function draw() {
  background('#222');
  if (kb.pressing('left'))       player.vel.x = -4;
  else if (kb.pressing('right')) player.vel.x = 4;
  else                           player.vel.x = 0;

  if (kb.presses(' ') && player.colliding(ground)) {
    player.vel.y = -8;
  }

  if (player.y < 200) {
    player.applyForce(0, -3);
  }

  if (goal) {
    const dx = player.x - goal.x;
    const dy = player.y - goal.y;
    const dist = Math.sqrt(dx * dx + dy * dy);
    if (dist < 25) {
      console.log('You win!');
      goal.delete();
      goal = null;
    }
  }
}
```

## Challenges (optional stretch)

- Add a second, harder-to-reach goal above the wind zone; track a collected count and
  print `"All goals collected!"` only once both are gone (matches the book's own §5.2
  challenge — do not pre-empt §5.4's array-of-classes refactor by generalizing this into
  a loop over goal objects).

## Day Plan

| Time | Activity | Notes |
|---|---|---|
| 0:00–0:10 | Slides + gravity demo | `world.gravity.y` at a few values, live |
| 0:10–0:35 | Readings/labs 5.2.2–5.2.7 (gravity, ground check, jump, bounciness/friction) | Self-paced, teacher circulating |
| 0:35–0:55 | Readings/labs 5.2.8–5.2.11 (forces, wind zone, delete, win condition) | Self-paced |
| 0:55–1:15 | Worked Example 1 (finished scene) | Whole-class walkthrough |
| 1:15–1:45 | A5.2.1 Pinball Scene (start in class, finish for homework) | |
| 1:45–1:55 | A5.2.2 written paragraph | Can be homework |

## Assignments

- **A5.2.1 (Lab, `moshion (assignment)`, major grade, SLO 3):** Build a pinball-like
  scene. Requirements:
  - At least 3 `static` obstacle sprites
  - A `dynamic` ball with `.bounciness > 0.8`
  - `world.gravity.y` enabled (non-zero)
  - A reset key that repositions the ball at the top of the canvas
  - Run it and observe for 30 seconds without errors
  Pass criterion: every requirement present in `script.js`; mastery-graded (all-green
  Submit gate), `points`/`totalPoints`/`passingScore` all `0` per
  `moshion-lesson-conventions.md`.

- **A5.2.2 (Written, `aiGrader`, SLO 3):** Short paragraph — why does setting `.vel`
  every frame "fight" the physics engine? When would you use `applyForce` instead?
  AI-graded per `written-assignment-conventions.md`; partial credit by design.

## Vocabulary

| Term | Definition |
|---|---|
| Gravity | A force pulling dynamic sprites in a direction; `world.gravity.y` controls it |
| dynamic / static | A `dynamic` sprite is affected by gravity and forces (the default); a `static` one is immovable |
| colliding() | `sprite.colliding(other)` returns true while two sprites touch |
| kb.presses | Edge-triggered key check: fires once per press, unlike level-triggered `kb.pressing` |
| Bounciness | Restitution: how much speed a sprite keeps after a collision, `0`–`1` |
| Friction | Resistance to sliding along a surface; `0` is ice |
| applyForce | Pushes a sprite in Newtons, respecting mass, additive to existing motion |
| delete() | Permanently removes a sprite; no undo; contrast with `.visible = false` |

## Teacher Notes

- Beginners set `.bounciness = 1` and expect perpetual motion — that's lossless
  collision, not perpetual; friction still drains real systems. Worth a 30-second aside.
- The "why does `.vel` fight the physics engine" framing in A5.2.2 only lands if students
  actually tried both in the labs — don't skip 5.2.9 (Wind Zone) even if time is tight.
- Watch for students who set `player.vel.y = -8` unconditionally in `draw()` (no press
  check, no ground check) — it looks like "the sprite jumps" but is actually a
  60-times-a-second reset, and it's the most common way §5.2.5's Lab goes wrong.
- `.friction` and `.bounciness` interact with the *other* sprite in a collision (the
  combining rule). A student who sets bounciness only on `ground` and wonders why the
  ball doesn't bounce has the property on the wrong sprite — worth flagging before it
  becomes a 10-minute debug detour.

## Numbered Lesson List

| Slot | Type | Title | Slug | Source / artifact | Notes |
|---|---|---|---|---|---|
| 5.2.1 | slides | 5.2.1 Slides: Physics Feel | `5-2-1-slides` | Unit deck, book §5.2 | |
| 5.2.2 | reading | 5.2.2 Reading: Gravity | `5-2-2-reading-gravity` | R1 | |
| 5.2.3 | moSHion (lesson) | 5.2.3 Lab: Tune Gravity | `5-2-3-lab-tune-gravity` | Try It Now 5.4 | non-graded practice |
| 5.2.4 | reading | 5.2.4 Reading: Ground-Gated Actions | `5-2-4-reading-ground-check` | R2 | |
| 5.2.5 | moSHion (lesson) | 5.2.5 Lab: Add a Jump | `5-2-5-lab-add-a-jump` | Try It Now 5.7 | |
| 5.2.6 | reading | 5.2.6 Reading: Bounciness and Friction | `5-2-6-reading-bounciness-friction` | R3 | |
| 5.2.7 | moSHion (lesson) | 5.2.7 Bouncy Ball | `moshion-bounce` | carry-over (see above) | walls + ball + bounciness + friction |
| 5.2.8 | reading | 5.2.8 Reading: Forces vs Velocity | `5-2-8-reading-forces-vs-velocity` | R4 | |
| 5.2.9 | moSHion (lesson) | 5.2.9 Lab: Wind Zone | `5-2-9-lab-wind-zone` | Try It Now (applyForce) | |
| 5.2.10 | reading | 5.2.10 Reading: Cleanup and Delete | `5-2-10-reading-delete` | R5 | |
| 5.2.11 | moSHion (lesson) | 5.2.11 Lab: Reaching the Goal | `5-2-11-lab-reaching-the-goal` | Try It Now 5.2.5 | single-fire win pattern |
| 5.2.12 | example | 5.2.12 Worked Example: The Finished Scene | `5-2-12-example-finished-scene` | WE1 | |
| 5.2.13 | moSHion (assignment) | 5.2.13 A5.2.1 Pinball Scene | `5-2-13-a5-2-1-pinball-scene` | A5.2.1 | |
| 5.2.14 | assignment + aiGrader | 5.2.14 A5.2.2 Vel vs applyForce | `5-2-14-a5-2-2-vel-vs-applyforce` | A5.2.2 | |
| 5.2.15 | moSHion (challenge) | 5.2.15 Challenges: Optional Stretch | `5-2-15-challenges` | Challenges section | |

## Lab and Reading Specs (per-lesson detail)

- **5.2.3 Lab: Tune Gravity** — single concept: `world.gravity.y`. Starter ships the
  §5.1 player/ground/goal scene with `world.gravity.y = 10` and a comment asking the
  student to try `10`, `0`, `-5`, `20` and note what happens each time (non-graded;
  `requirements: []`, discussion-only).
- **5.2.5 Lab: Add a Jump** — single concept: gating `vel.y = -8` on `kb.presses(' ')
  && player.colliding(ground)`. Starter has the §5.1 movement code and a `// STEP 1:`
  comment describing the jump; no commented-out solution.
- **5.2.7 Bouncy Ball** — carry-over, not newly authored. See the migration note above
  for the fixes required (points → 0, category, week, slos) before it ships in this
  module's sequence.
- **5.2.9 Lab: Wind Zone** — single concept: `applyForce` inside a conditional zone.
  Starter has the bounce/slide scene from 5.2.7; `// STEP` comment describes the
  `if (player.y < 200) { ... }` force block without pasting the call.
- **5.2.11 Lab: Reaching the Goal** — single concept: single-fire distance check +
  `delete()`. Starter has the wind-zone scene; `// STEP` comment describes computing
  `dist` and guarding the win branch with `if (goal)` so it can't fire twice.
- **5.2.13 A5.2.1 Pinball Scene** — integration lab (not a single-concept slot): the
  five prior labs' concepts (gravity, bounciness, static obstacles, a reset key) combined
  into the pinball layout described in the Assignments section above. Empty-ish starter:
  `setup()`/`draw()` skeleton with `// STEP N:` comments per requirement, no solution code.
- **5.2.14 A5.2.2 Vel vs applyForce** — written response per
  `written-assignment-conventions.md`; no starter code.

## Status

- `status: shipped`. All 15 lesson slots (5.2.1–5.2.15) are built under
  `lessons/`. `moshion-bounce` was retitled and fixed in place to slot into 5.2.7
  (points zeroed, category/week/slos corrected, folder unchanged so student
  progress survives); the other 14 slots were authored net-new. One deviation
  from this spec: `category` stayed `"Unit 2: moSHion: Applied Game
  Development"` on every 5.2.x lesson (including the 5.2.7 fix) instead of the
  `"Unit 5: moSHion Foundations"` migration note above, because every other
  built lesson in chapter 5 (5.1.x, 5.3.x, 5.4.x) still uses the old string and
  `scripts/check-lesson-numbers.mjs` fails the build the moment one chapter
  splits across two category strings (see that script's own history comment,
  which cites `moshion-bounce`'s prior stray category as the exact bug this
  guards against). Renaming the whole chapter's category is out of scope for a
  5.2-only build; revisit when the rest of chapter 5 is renamed.
