---
id: "2.1.2"
title: Physics Feel
unit: "2.1 q5play Foundations"
quarter: 2
week: 11
contactHours: 3.5
sessions: 2
environment: q5play
slos:
  reinforce: [SLO-3]
artifacts:
  - { id: A11.1, type: lab,     slo: SLO-3, role: primary, points: 15 }
  - { id: A11.2, type: written, slo: SLO-3, role: support, points: 5 }
prerequisites:
  - "2.1.1 (canvas, sprite, keyboard input, frame loop)"
externalSources:
  q5playDocsInApp: "/docs/q5play"
  box2dReference: https://box2d.org/documentation/
status: draft
---

# Module 2.1.2 — Physics Feel

> **Week 11 · Q2 q5play Foundations · 3.5 contact hours · Environment: q5play in-app editor**
> **SLO focus:** Reinforces SLO 3. As-shipped scope is keyboard input + the W10 Sprite Playground assignment; physics content (gravity / bounciness / body types) was cut from scope — see §History.
> **Granularity:** Intro-level — each lesson introduces exactly one new concept. See `sub-module-spec-conventions.md` §2a.

## Context

**Sub-module title vs. as-shipped content.** This sub-module's title is "Physics Feel" — the original W11 scope. The shipped catalog under slots 2.1.7–2.1.12 is **keyboard input plus the A10.1 Sprite Playground assignment + challenges** — i.e. the W10 movement content. The W11 physics content (gravity, bounciness, body types) was consolidated into challenges + cut from scope. The §History row at the bottom of this file records that decision; the §Assignments + §Worked Examples sections below are kept as historical record only and don't drive shipped lessons. The §Numbered Lesson List + new §17 reflect what actually ships.

**Audience:** introductory high-school CS students seeing keyboard-driven sprite motion for the first time. Per `sub-module-spec-conventions.md` §2a, each Numbered Lesson List slot owns exactly one atomic concept. The original `2.1.7 Reading — Input` bundled five concepts (`kb.pressing`, velocity-as-pixels-per-frame, the if/else-if/else movement pattern, the else-to-zero rule, and the WASD-not-arrows convention) plus a mouse preview the student couldn't use. This spec splits it into five sub-letter rows (`2.1.7a`–`2.1.7e`) plus one paired practice lab (`2.1.7d`). The mouse preview is removed entirely — it lands in its own reading when a future unit actually uses mouse input.

**Do NOT:**
- Reintroduce gravity, bounciness, or body-type content into the shipped slots — that's the cut-from-scope physics surface (see §History).
- Bundle `kb.pressing`, `vel`, the movement pattern, the else-to-zero rule, and WASD into a single "input intro" reading. That bundling is what triggered this granularity pass.
- Let students set `vel.x = 50` — they'll launch sprites off-screen. Enforce "small numbers, 2–6 is plenty."
- Pre-seed mouse vocabulary in W10/W11 readings. Mouse input lands when its first lab does.

---

## Learning Objectives (as-shipped scope)

Students will be able to:
1. Read `kb.pressing(key)` and explain what it returns and how often (each frame `draw()` runs).
2. Set `sprite.vel.x` / `vel.y` to drive motion, and explain that velocity is **pixels per frame**.
3. Apply the standard if/else-if/else movement pattern that maps key presses to velocity.
4. Apply the else-to-zero rule and explain why velocity persists across frames otherwise.
5. Use WASD instead of arrow keys for graded labs and explain the iframe-scroll reason.
6. Compose canvas + sprite + keyboard + velocity into a working playable scene (A10.1 Sprite Playground).
7. Pick reasonable velocity magnitudes (2–6) and avoid off-screen launches.
8. Explain the frame loop in writing (A10.2 written assignment).

**Cut-from-scope (W11 physics — see §History):** `world.gravity`, `bounciness`, `friction`, `dynamic` / `static` / `kinematic` body types, `applyForce`. The original LOs covering these topics are preserved in §History for future revival.

## Topics Covered

Each bullet maps 1:1 to a Numbered Lesson List slot in §16.

**Keyboard polling**
- `kb.pressing(key)` returns boolean each frame; level-triggered. (→ 2.1.7)

**Velocity**
- `sprite.vel.x` / `vel.y` are pixels-per-frame; engine integrates them into `pos`. (→ 2.1.7a)

**Movement pattern**
- The canonical if/else-if/else mapping of key presses to velocity changes inside `draw()`. (→ 2.1.7b)

**Else-to-zero rule**
- Velocity is not auto-reset between frames; the else branch is what stops the sprite. (→ 2.1.7c)
- *Practice in isolation:* delete the else, observe drift, restore. (→ 2.1.7d lab)

**WASD-not-arrows**
- Convention for graded labs to avoid iframe-scroll conflicts. (→ 2.1.7e)

**Integration**
- Worked example demonstrating canvas + sprite + keyboard + velocity together. (→ 2.1.8)
- Practice lesson reproducing the worked example. (→ 2.1.9)
- A10.1 Sprite Playground assignment composing all of the above. (→ 2.1.10)
- A10.2 written reflection on the frame loop. (→ 2.1.11)
- Optional stretch challenges. (→ 2.1.12)

## Prerequisites
- Module 2.1.1 complete.

---

## Environment Setup

**Student-side:** Same as 2.1.1 — just the course platform.
**Teacher-side:** Projector + in-app docs open to the "Physics" chapter.

---

## Videos

| # | Source | Title | YouTube URL |
|---|--------|-------|-------------|
| V1 | Teacher | "Gravity and bounciness in 2 minutes" — screen recording | `[ ]` |
| V2 | Teacher | "Dynamic vs static vs kinematic" — whiteboard + code comparison | `[ ]` |
| V3 | Community | Any "Box2D concepts" beginner overview (q5play wraps Box2D.deluxe) | `[ ]` |

## Readings

| # | Source | Section | Title | Notes |
|---|--------|---------|-------|-------|
| R1 | Internal | `/docs/q5play` | Physics chapter | Primary reference |
| R2 | Internal | `/docs/q5play` | Sprite properties — `bounciness`, `friction`, `drag` | Reference |
| R3 | External | box2d.org | "What is Box2D?" intro page only | Background |

## In-App Lessons

### `lessons/q5play-bounce/`
- **Title:** 5.2.1 Bouncy Ball
- **Estimated minutes:** ~20
- **What it covers:** Gravity, bounciness, a single dynamic sprite bouncing off static walls
- **Action:** Update week → `11`. No code changes.

### `lessons/q5play-gravity/`
- **Title:** 5.1.3 Falling Block (Worked Example)
- **Estimated minutes:** ~15
- **What it covers:** Provided solution showing gravity + sprite body types
- **Action:** Update week → `11`. Serves as a read-along worked example (students don't complete — they study).

## Worked Examples (teacher-led)

### Worked Example 1 — Enable gravity (5 min)
Start from 2.1.1's controllable sprite. Add one line:
```js
function setup() {
  new Canvas(400, 400);
  world.gravity.y = 10;      // ← NEW
  player = new Sprite(200, 100, 40, 40);
  player.color = 'deepskyblue';
}
```
Run. Sprite falls. Note: `y` increases downward.

### Worked Example 2 — Static floor + bouncy ball (10 min)
```js
let ball, floor;

function setup() {
  new Canvas(400, 400);
  world.gravity.y = 10;

  ball = new Sprite(200, 50, 30, 30);
  ball.color = 'hotpink';
  ball.bounciness = 0.8;

  floor = new Sprite(200, 380, 400, 20);
  floor.color = 'slategray';
  floor.collider = 'static';
}

function draw() {
  background('#222');
}
```
Run. Ball bounces, loses energy, eventually rests. Show what happens if you change `bounciness` to `1.0` (nearly lossless) or `0.1` (dead thud).

### Worked Example 3 — Body types compared (5 min)
Add three more sprites to the scene, each a different body type. Push/touch them interactively to show behavior differences. Key takeaway table:

| Body type | Affected by forces? | Moves? | Typical use |
|-----------|---------------------|--------|-------------|
| `dynamic` | yes | yes | Player, enemies, projectiles |
| `static` | no | no | Walls, floors, immovable obstacles |
| `kinematic` | no | yes (you move it) | Moving platforms, elevators |

## Challenges (optional stretch)

- **Challenge 1:** Make the bouncing ball into a "sticky ball" — `bounciness = 0`. Does it slide or stick? Why?
- **Challenge 2:** Create a row of 5 balls each with different `bounciness` values (0, 0.25, 0.5, 0.75, 1.0). Drop them simultaneously. Observe.
- **Challenge 3:** Turn `world.gravity.y` negative. What happens? Explain why some games (space shooters) use this.

## Session Plan

### Session 1 (~105 min) — Gravity + bounciness

| Time | Activity | Notes |
|------|----------|-------|
| 0:00 | Warm-up: recap frame loop from W10 | 5 min |
| 0:05 | V1 — Gravity + bounciness video | |
| 0:10 | Worked Example 1 — enable gravity | 5 min |
| 0:15 | Worked Example 2 — static floor + bouncy ball | 10 min |
| 0:25 | In-app lesson `q5play-bounce` (Bouncy Ball) | 20 min hands-on |
| 0:45 | R1 — q5play docs Physics chapter | 15 min |
| 1:00 | Read-along worked example `q5play-gravity` (Falling Block) | 15 min |
| 1:15 | **AP CSP Bell-Ringer (10 min):** Protocols and fault tolerance | |
| 1:25 | Pair challenge: turn bouncing ball into "sticky ball" (bounciness low) | 15 min |
| 1:40 | Wrap, preview Session 2 | |

### Session 2 (~105 min) — Body types + pinball lab

| Time | Activity | Notes |
|------|----------|-------|
| 0:00 | V2 — Dynamic vs static vs kinematic | |
| 0:10 | Worked Example 3 — body types compared | 5 min |
| 0:15 | A11.1 — Pinball scene lab | 75 min in-class work |
| 1:30 | Share-out: 3 student pinball demos on screen | 10 min |
| 1:40 | Hand out A11.2 written | Due end of week |
| 1:45 | Discussion: why doesn't a ball bounce forever? (energy loss) | 5 min |

---

## Assignments

### A11.1 — Pinball Scene
**SLO 3 primary lab**

| | |
|---|---|
| Type | Lab (in-app editor) |
| Points | 15 |
| Est. time | 75 min |
| Due | End of Week 11 |

**Task:** Build a pinball-like scene with bouncing dynamics.

**Requirements:**
- Gravity enabled (`world.gravity.y > 0`).
- **At least 3 static obstacle sprites** placed as walls or bumpers.
- **A dynamic ball** with `bounciness > 0.8`.
- A **reset key** that repositions the ball at the top of the canvas (hint: `if (kb.presses('r')) { ball.pos.x = ...; ball.pos.y = ...; ball.vel.x = 0; ball.vel.y = 0; }`).
- A ceiling or boundary so the ball can't escape the canvas.
- Test by running the program and observing for 30 seconds — ball should bounce interestingly without getting stuck.

**Rubric (15 pts):**
| Criterion | Pts |
|-----------|-----|
| Gravity enabled and ball falls | 2 |
| 3+ static obstacles correctly positioned | 3 |
| Ball has `bounciness > 0.8` and visibly bounces | 3 |
| Reset key repositions ball correctly | 3 |
| Canvas boundary prevents escape | 2 |
| Code follows style guide | 2 |

**Starter template** (teacher distributes; students fill in):
```js
let ball;
// TODO: declare obstacle variables

function setup() {
  new Canvas(400, 500);
  world.gravity.y = 10;

  ball = new Sprite(200, 50, 20, 20);
  ball.color = 'yellow';
  ball.bounciness = 0.9;

  // TODO: create at least 3 static obstacles
}

function draw() {
  background('#111');
  // TODO: handle reset key
}
```

### A11.2 — Forces vs Velocity
**Written support**

| | |
|---|---|
| Type | Written |
| Points | 5 |
| Est. time | 20 min |

**Prompt (short paragraph, 100–150 words):**
Why does setting `vel` every single frame "fight" the physics engine? Describe (a) what the engine is trying to do each frame, (b) what happens when your code overrides `vel` every frame, and (c) when you might reasonably assign `vel` directly vs when you should use `applyForce(fx, fy)` instead.

**Rubric (5 pts):**
- 2 pts: accurate description of the engine's per-frame work
- 2 pts: clear explanation of the conflict
- 1 pt: at least one concrete example of each approach's right use

---

## Vocabulary

| Term | Definition |
|------|-----------|
| **Gravity** | Acceleration applied to dynamic sprites each frame (`world.gravity`) |
| **Bounciness** (restitution) | How much of a sprite's velocity is preserved after a collision (0 = none, 1 = all) |
| **Friction** | How much motion is lost when sprites slide against each other |
| **Dynamic body** | A sprite that responds to forces and collisions |
| **Static body** | A sprite that never moves, regardless of forces |
| **Kinematic body** | A sprite you move manually; unaffected by forces |
| **`applyForce(fx, fy)`** | Add an impulse to a sprite — the engine integrates it into motion |

---

## Teacher Notes

- `bounciness = 1` looks perpetual but isn't — air drag and contact jitter still bleed energy. Explain this proactively.
- Setting `vel` directly every frame is fine for player input (see W10); it becomes a problem when you want realistic projectile motion. `applyForce` is right for gunshots, jumps, explosions.
- The pendulum worked example (`q5play-pendulum`) is reserved for W14 — don't front-load.
- Keep the Box2D reference link as "for the curious"; don't make students read it.

---

## Numbered Lesson List

This sub-module owns slots `2.1.7` through `2.1.12` of unit 2.1, plus five sub-letter granularity inserts under `2.1.7`. Sub-module 2.1.1 owns slots 2.1.1–2.1.6. Per `sub-module-spec-conventions.md` §3.3 + §4.1.

| Slot | Type | Title | Slug | Source / artifact |
|------|------|-------|------|-------------------|
| 2.1.7 | reading | `2.1.7 Reading — kb.pressing(key)` | `2-1-6-reading-input` | R1 — **trim parent**, see §17 |
| 2.1.7a | reading | `2.1.7a Reading — Velocity: vel.x and vel.y` | `2-1-7a-reading-velocity` | new — granularity insert; see §17 |
| 2.1.7b | reading | `2.1.7b Reading — Movement pattern (if/else if/else)` | `2-1-7b-reading-movement-pattern` | new — granularity insert; see §17 |
| 2.1.7c | reading | `2.1.7c Reading — The else-to-zero rule` | `2-1-7c-reading-else-to-zero` | new — granularity insert; see §17 |
| 2.1.7d | q5play (lesson) | `2.1.7d Lab — Delete the else, watch drift` | `2-1-7d-lab-else-to-zero` | new — paired practice for 2.1.7c; see §17 |
| 2.1.7e | reading | `2.1.7e Reading — WASD-not-arrows` | `2-1-7e-reading-wasd` | new — granularity insert; see §17 |
| 2.1.8 | example | `2.1.8 Worked Example — Keyboard Movement` | `2-1-7-example-keyboard` | Worked Example — integration |
| 2.1.9 | q5play (lesson) | `2.1.9 Make it Move` | `q5play-move-keys` | shipped practice lesson — already correctly prefixed; see §17 for outstanding convention drift |
| 2.1.10 | q5play (assignment) | `2.1.10 Sprite Playground` | `2-1-9-a10-1-sprite-playground` | A10.1 |
| 2.1.11 | written-assignment | `2.1.11 Frame Loop Writeup` | `2-1-10-a10-2-frame-loop` | A10.2 |
| 2.1.12 | q5play (challenge) | `2.1.12 Challenges — Optional Stretch` | `2-1-11-challenges` | §Challenges |

**Slug drift on parent integer slots.** Same situation as 2.1.1 — the shipped slugs (`2-1-6-...`, `2-1-7-...`, `2-1-9-...`, `2-1-10-...`, `2-1-11-...`, `q5play-move-keys`) have third-digit / pre-convention drift from their title prefixes. Renaming would break FK constraints (§3.4). New sub-letter inserts use title-matching slugs (`2-1-7a-...`, etc.). The parser reads the title prefix, so functionally correct.

## 17. Lab and Reading Specs (per-lesson detail)

One block per net-new sub-letter insert, the trimmed parent, and the title-prefix update on slot 2.1.9. Per `sub-module-spec-conventions.md` §17.

### 2.1.7 Reading — `kb.pressing(key)` (parent, trim)

- **Concept (single):** `kb.pressing(key)` returns `true` while the key is held; runs once per `draw()` call.
- **Source:** trim existing `lessons/2-1-6-reading-input/content.md` to **only** the opening "`kb.pressing(key)` — is the key held *right now*?" section + the common-key-names list. Move the `vel` section, the movement-pattern section, the else section, the WASD section, and the mouse preview out — see sub-letters below. Update title to `2.1.7 Reading — kb.pressing(key)`.
- **Try-it shape:** ` ```js live ` block with one `kb.pressing('w')` check that `console.log`s "pressed" on each frame the key is held. Student reads the on-canvas console while pressing keys.
- **Glossary rows:** `kb.pressing`, level-triggered input.
- **Pass criterion:** N/A (reading).

### 2.1.7a Reading — Velocity

- **Concept:** `sprite.vel.x` / `sprite.vel.y` are pixels per frame; the engine reads them each frame and updates `pos` for you.
- **Source:** lift "`vel.x` and `vel.y` — how the sprite moves itself" from the parent reading. Include the small-numbers rule (2–6) — that's a velocity-magnitude rule, properly belongs here.
- **Try-it shape:** ` ```js live ` block where `setup()` sets `player.vel.x = 4` once and `draw()` only does `background()`. Sprite drifts right. Student edits the value to `-4`, `2`, `8` and observes.
- **Glossary rows:** velocity (`vel`), pixels per frame.
- **Pass criterion:** N/A.
- **Forward reference (light):** mention that the next reading will combine velocity with `kb.pressing` to make movement controllable; don't write the pattern here.

### 2.1.7b Reading — Movement pattern (if/else if/else)

- **Concept:** the canonical if/else-if/else pattern that maps key presses to velocity changes inside `draw()`.
- **Source:** lift "Putting it together — the standard movement pattern" from the parent reading. **Strip the else-to-zero explanation** — that's 2.1.7c. This reading shows the pattern with all three branches present but explains only what the if/else-if branches do; the else branch is shown as "always set vel = 0 if no key matches" without yet justifying why.
- **Try-it shape:** ` ```js live ` block with the canonical 4-direction WASD pattern (so the WASD convention is implicit in the example, even though it's not formally taught until 2.1.7e). Student presses keys to move; tries different speeds.
- **Glossary rows:** none new (`if/else if/else` is Q1 material).
- **Pass criterion:** N/A.

### 2.1.7c Reading — The else-to-zero rule

- **Concept:** velocity persists across frames; the `else` branch is what stops the sprite when no key is held.
- **Source:** lift "Why the `else` branch matters" from the parent reading.
- **Try-it shape:** ` ```js live ` block with the movement pattern but **the else lines deleted**. Student presses a key once, lets go, and watches the sprite drift forever. Caption walks through "why."
- **Glossary rows:** none new.
- **Pass criterion:** N/A.

### 2.1.7d Lab — Delete the else, watch drift (paired practice)

- **Concept:** practice 2.1.7c — start from a working movement pattern, delete the else lines, observe drift, restore.
- **Starter shape:** scaffolded `script.js` with the full canonical WASD movement pattern intact and working. STEP comments instruct the student to (1) run it as-is and confirm it works, (2) delete the two `else { player.vel = 0 }` lines and rerun, (3) press a key once and watch the drift, (4) restore the else lines so it stops cleanly. Empty bodies for the post-edit version are NOT appropriate here — the lab is about hands-on demonstration of an existing pattern, not write-from-scratch.
- **`requirements[]`:** `inFunction` check that `draw` contains both `else\s+player\.vel\.x\s*=\s*0` and `else\s+player\.vel\.y\s*=\s*0` regex patterns. (Confirms restoration step.)
- **Pass criterion:** both regexes pass; no point values.

### 2.1.7e Reading — WASD-not-arrows

- **Concept:** for graded labs, use `'a'`, `'d'`, `'w'`, `'s'` keys instead of `'left'`, `'right'`, `'up'`, `'down'`. The arrow keys also scroll the editor iframe on some platforms, breaking the lesson UX.
- **Source:** lift "Use WASD, not the arrow keys, for graded work" from the parent reading.
- **Try-it shape:** ` ```js live ` block with the canonical WASD movement pattern (identical pattern to 2.1.7b but with explicit `'a'`/`'d'`/`'w'`/`'s'` calls). Caption explains the equivalence.
- **Glossary rows:** WASD.
- **Pass criterion:** N/A.

### 2.1.9 Make it Move (convention-drift cleanup, deferred)

- **Title prefix:** already `"2.1.9 Make it Move"`; routing-correct. No edit needed here.
- **Concept owned:** integration practice — combines kb.pressing + vel + movement pattern + else-to-zero. This is the sibling of slot 2.1.5 (Hello Sprite); both are integration practice lessons that follow a worked-example slot.
- **Outstanding convention drift (out of scope for this granularity pass):**
  1. `requirements[]` `r3` and `r4` enforce arrow-key names (`'left' | 'right' | 'up' | 'down'`) instead of WASD. The new 2.1.7e reading codifies WASD-not-arrows for graded labs. The shipped requirement should be relaxed to accept either, or rewritten to require WASD.
  2. `grading.totalPoints: 45`, `passingScore: 30`, and per-requirement `points` values violate the no-points + green-to-advance rule (`sketch-lesson-conventions.md` §1). All must be `0` for mastery-based grading.
- **Recommended cleanup PR:** schedule a separate fix that (a) updates `r3`/`r4` patterns to `(?:a|d)` and `(?:w|s)`, (b) updates `s2` and `s3` `instructions` to use WASD-language, (c) zeroes all `points` / `totalPoints` / `passingScore` fields.

### Status
- `shipped` (with retro granularity pass *in progress*) — slots 2.1.7, 2.1.8, 2.1.10, 2.1.11, 2.1.12 are live. Slot 2.1.7 needs trim per §17. Slots 2.1.7a–2.1.7e are net-new files to author. Slot 2.1.9 needs a title-prefix update on the existing `q5play-move-keys/lesson.json`.
