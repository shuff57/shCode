---
id: "2.3.2"
title: Physics Applications
unit: "2.3 Collections and Physics Applications"
quarter: 2
week: 14
contactHours: 3.5
sessions: 2
environment: q5play
slos:
  reinforce: [SLO-3]
artifacts:
  - { id: A14.1, type: lab, slo: SLO-3, role: primary, points: 20 }
prerequisites:
  - "2.1.2 physics, 2.2.1 classes, 2.3.1 groups — students have all building blocks"
status: draft
---

# Module 2.3.2 — Physics Applications

> **Week 14 · Q2 Collections · 3.5 contact hours (2 sessions) · Environment: q5play in-app editor**
> **SLO focus:** Reinforces SLO 3. Consolidation week before the W14 midterm.

## Context

Week 14 is **consolidation, not new concepts**. Students combine inputs, physics, and groups to build a playable scene with a win condition. Two challenge options: Space Jumper (easier, platformer-ish) or Car on a Ramp (harder, dynamics-heavy). Pendulum worked example seeds joints thinking for W17. The S1 Midterm 3 is administered either at the start or end of this week (teacher discretion).

**Do NOT:**
- Introduce new mechanics — everything this week uses already-learned tools.
- Let students restart from scratch when a physics tuning goes wrong — debug by adjusting ONE property.

---

## Learning Objectives

Students will be able to:
1. Combine input, physics, and collision to build a playable scene.
2. Distinguish `kb.presses(...)` (edge-triggered, one-shot) from `kb.pressing(...)` (level-triggered, continuous).
3. Implement a jump using an impulse force or a sudden `vel.y` change.
4. Check whether a sprite is "on the ground" before allowing a jump.
5. Debug interactions between input-driven and physics-driven motion.

## Topics Covered
- `kb.presses('space')` — fires exactly once on each key-down edge.
- Impulse forces for jumping: `player.vel.y = -jumpStrength` or `applyForce(0, -impulse)`.
- Ground detection: `sprite.touching` or overlap with a ground group.
- Static platforms as level geometry.
- Slopes and angular motion (seeds W17 joints).

## Prerequisites
- Modules 2.1.2 (physics), 2.2.1 (classes), 2.3.1 (groups) complete.

---

## Videos

| # | Source | Title | YouTube URL |
|---|--------|-------|-------------|
| V1 | Teacher | "kb.presses vs kb.pressing" (2 min) | `[ ]` |
| V2 | Teacher | "Why your jump feels wrong" (3 min — ground detection bug walk) | `[ ]` |
| V3 | Community | "What makes a jump feel right?" (coyote time, variable height — game design classic) | `[ ]` |

## Readings

| # | Source | Section | Notes |
|---|--------|---------|-------|
| R1 | Internal | `/docs/q5play` — Input chapter | `presses` vs `pressing` |
| R2 | Internal | `/docs/q5play` — Collisions chapter | `touching`, `overlaps` |

## In-App Lessons

### `lessons/q5play-jump-challenge/`
- **Title:** 5.1.5 Space Jumper (Challenge)
- **Est. time:** ~35 min
- **Role:** Challenge option A (easier).
- **Action:** Update week → `14`.

### `lessons/q5play-car-challenge/`
- **Title:** 5.2.3 Car on a Ramp (Challenge)
- **Est. time:** ~40 min
- **Role:** Challenge option B (harder).
- **Action:** Update week → `14`.

### `lessons/q5play-pendulum/`
- **Title:** 5.2.2 Swinging Pendulum (Worked Example)
- **Est. time:** ~20 min
- **Role:** Read-along worked example. Previews joints (W17).
- **Action:** Update week → `14`.

## Worked Examples

### Worked Example 1 — presses vs pressing (5 min)
```js
function draw() {
  // WRONG — fires 60 times while held, causes super-jump
  if (kb.pressing('space')) player.vel.y = -10;

  // RIGHT — fires once per press
  if (kb.presses('space')) player.vel.y = -10;
}
```

### Worked Example 2 — Ground detection (10 min)
```js
let player, ground;

function setup() {
  new Canvas(600, 400);
  world.gravity.y = 20;
  ground = new Sprite(300, 390, 600, 20);
  ground.collider = 'static';
  player = new Sprite(100, 300, 30, 30);
}

function draw() {
  background('#222');
  if (kb.pressing('left'))  player.vel.x = -5;
  else if (kb.pressing('right')) player.vel.x = 5;
  else player.vel.x = 0;

  // Only allow jump when touching ground
  if (kb.presses('space') && player.touching(ground)) {
    player.vel.y = -12;
  }
}
```

### Worked Example 3 — Pendulum preview (10 min, read-along)
Open `lessons/q5play-pendulum/` together. Point at where the joint code will eventually live (hidden or commented). Tell students: "You'll write this in W17. Today, just read it."

## Challenges

All students must complete ONE of the two challenges as A14.1:

- **Option A (easier):** `q5play-jump-challenge` (Space Jumper)
- **Option B (harder):** `q5play-car-challenge` (Car on a Ramp)

Advanced students: try both.

## Session Plan

### Session 1 (~105 min) — Input edges + pick a challenge

| Time | Activity |
|------|----------|
| 0:00 | V1 + Worked Example 1 — presses vs pressing |
| 0:15 | V2 + Worked Example 2 — ground detection |
| 0:35 | R1 + R2 readings |
| 0:50 | Read-along worked example `q5play-pendulum` |
| 1:10 | Intro to challenges — students pick A or B |
| 1:20 | Begin challenge work |
| 1:45 | Wrap |

### Session 2 (~105 min) — Finish challenge + midterm

| Time | Activity |
|------|----------|
| 0:00 | S1 Midterm 3 (1 hour) — covers Units 2.1–2.3 |
| 1:00 | Finish A14.1 challenge |
| 1:40 | Share-out + wrap |

---

## Assignments

### A14.1 — Challenge: Space Jumper OR Car on a Ramp
**SLO 3 primary**

| | |
|---|---|
| Type | Lab (in-app editor — choose one challenge) |
| Points | 20 |
| Est. time | 60–90 min |
| Due | End of Week 14 |

**Requirements (regardless of option):**
- At least one static obstacle or piece of level geometry.
- At least one dynamic player sprite.
- Working input-driven motion (keyboard).
- Physics correctly applied (gravity on, appropriate body types).
- A visible win condition (flag, reach-the-top marker, cross finish line).
- Bonus: lose/restart condition.

**Rubric (20 pts):**
| Criterion | Pts |
|-----------|-----|
| Scene renders and runs without errors | 2 |
| Input-driven motion works cleanly | 4 |
| Physics feels reasonable (no sprite through walls, no infinite-jumping) | 4 |
| Static geometry correct | 2 |
| Win condition fires correctly | 4 |
| Code follows style guide (class style guide from W3) | 2 |
| README describing which challenge + one design choice made | 2 |

Choice of Option A vs Option B does not affect grading.

---

## Vocabulary

| Term | Definition |
|------|-----------|
| **Edge-triggered input** | `kb.presses(k)` — fires exactly once on each key-down |
| **Level-triggered input** | `kb.pressing(k)` — returns true every frame while held |
| **Impulse** | A sudden, one-frame velocity change (as opposed to continuous force) |
| **Ground detection** | Checking whether a sprite is in contact with a ground surface |
| **`sprite.touching(other)`** | Returns true if the sprite is touching `other` this frame |

---

## Teacher Notes

- The midterm falls this week — schedule it for Session 2 first hour so students can finish A14.1 second hour.
- Most students will pick Space Jumper. Redirect confident students to Car on a Ramp.
- The Pendulum read-along is a SEED for W17, not a requirement this week. Don't test on it.
- If a student can't get their jump to feel right, adjust ONE property at a time — gravity, jump impulse, drag. Changing multiple at once masks which one fixed it.

---

## Build Outputs

> **Per-type conventions** — every artifact below follows the convention doc for its type. Full index: [`curriculum/resources/README.md`](../../resources/README.md). Most relevant for this module:
> - q5play starters → [`q5play-starter-conventions.md`](../../resources/q5play-starter-conventions.md)
> - Lab assignment A14.1 (choose-your-option: `q5play-jump-challenge` OR `q5play-car-challenge`) → [`lab-assignment-conventions.md`](../../resources/lab-assignment-conventions.md) and [`q5play-challenge-conventions.md`](../../resources/q5play-challenge-conventions.md)
> - Worked example (`q5play-pendulum` read-along) → [`example-conventions.md`](../../resources/example-conventions.md)
> - Video manifest → [`video-conventions.md`](../../resources/video-conventions.md)
> - Unit 2.3 slide deck (shared with 2.3.1) → [`slide-deck-conventions.md`](../../resources/slide-deck-conventions.md)

When the user says **"build 2.3.2"**, produce:

1. **A14.1 assignment markdown:** `assignments/A14.1_physics-challenge.md` — rubric, pointer to the two lesson paths, explanation of choose-your-option structure.
2. **Update lesson metadata:** week → 14 for `q5play-jump-challenge`, `q5play-car-challenge`, `q5play-pendulum`.
3. **Midterm 3 exam:** `exams/S1-midterm-3.md` — 5 MC + 3 code-reading + 2 short-answer + 1 write-code problem covering Units 2.1–2.3. Include answer key at bottom, marked teacher-only.
4. **Video manifest:** `assignments/2.3.2_video-manifest.md`.

### Do NOT
- Modify script files in the existing lessons.
- Ship a midterm that covers W15+ content.

## Status
- `draft`
