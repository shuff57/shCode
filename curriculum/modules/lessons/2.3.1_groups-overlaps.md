---
id: "2.3.1"
title: Groups and Overlaps
unit: "2.3 Collections and Physics Applications"
quarter: 2
week: 13
contactHours: 3.5
sessions: 2
environment: q5play
slos:
  reinforce: [SLO-3, SLO-4]
artifacts:
  - { id: A13.1, type: lab, slo: SLO-3, role: primary, points: 20 }
prerequisites:
  - "2.2.1 (classes) — students understand what an instance is before managing collections of them"
  - "Q1 W8 arrays"
externalSources:
  q5playDocsInApp: "/docs/q5play"
status: draft
---

# Module 2.3.1 — Groups and Overlaps

> **Week 13 · Q2 Collections · 3.5 contact hours (2 sessions) · Environment: q5play in-app editor**
> **SLO focus:** Reinforces SLO 3 (applied implementation) and SLO 4 (collision detection as an algorithm).

## Context

Week 13 transitions students from "one sprite" to "many sprites managed as a collection." The `Group` class is a specialized array with extra methods — the q5play idiom for managing enemies, collectibles, projectiles, platforms, etc. Students also learn `overlaps()` for collision detection and the safe patterns for spawn/despawn logic. The algorithmic thinking required (spawn condition, collision loop, remove-during-iteration) reinforces SLO 4.

**Do NOT:**
- Let students iterate forward with `for (let i = 0; i < group.length; i++)` AND call `remove` inside — the #1 bug this week. Teach iterate-backwards or collect-then-remove up front.
- Introduce `update()` separate from `draw()` — one function is enough for now.

---

## Learning Objectives

Students will be able to:
1. Create a `Group` and add sprites to it.
2. Iterate a group safely, including during removal.
3. Detect overlaps with `sprite.overlaps(group)` (boolean) and `sprite.overlaps(group, callback)` (per-pair).
4. Spawn sprites at random positions using `random()` / `Math.random()`.
5. Schedule periodic spawns using `frameCount % N === 0`.
6. Despawn sprites that leave the canvas.

## Topics Covered
- `new Group()` and how it extends Array.
- Automatic sprite-to-group association (pass the group as parent, or add explicitly).
- Iterating a group (forEach, for-of, classical for).
- `sprite.overlaps(other)` — returns boolean; callback form receives both sprites.
- `sprite.remove()` — despawns and removes from any groups.
- Spawn patterns: random position, timed spawn via `frameCount`, enemy waves.
- Collision detection as an algorithm — conceptual link to SLO 4.

## Prerequisites
- Module 2.2.1 complete.
- Q1 W8 arrays comfortable.

---

## Environment Setup

Standard q5play setup. Teacher has the `q5play-groups` docs chapter open on the projector.

---

## Videos

| # | Source | Title | Description hook | YouTube URL |
|---|--------|-------|------------------|-------------|
| V1 | Teacher | "Groups in q5play — a sprite array with superpowers" (4 min) | Why a `Group` is a class with shared defaults — and how a single line creates fifty enemies. Watch before `2.3.5 Groups Sandbox`. | `[ ]` |
| V2 | Teacher | "overlaps() — boolean vs callback" (3 min) | The two flavors of `overlaps()` and why the callback form is cleaner for despawn-on-collide. Watch before `2.3.7 Apple Catcher`. | `[ ]` |
| V3 | Teacher | "The remove-during-iteration bug" (2 min) | Live demo of why `for (let i = 0; i < group.length; i++)` + `remove` skips items, and how to fix it. Watch before `2.3.10 Safe Despawn`. | `[ ]` |

## Readings

| # | Source | Section | Title | Notes |
|---|--------|---------|-------|-------|
| R1 | Internal | `/docs/q5play` | Groups chapter | Primary reference for `Group`, defaults, iteration, factory syntax. |
| R2 | Internal | `/docs/q5play` | Collisions + Overlaps chapter | `overlaps()` boolean + callback forms; `remove()` semantics. |

### Reading content guidance

Both readings split into two `## <Topic>` subsections per `reading-conventions.md` §3 — each with a `js live` Try-it block and one row contributed to the closing glossary.

#### R1 — Groups (`2-3-3-reading-groups`)

- **Topic 1: Creating + iterating a Group** — read before `2.3.5 Groups Sandbox`.
  - **What you'll learn from it:** what `new Group()` returns; that a Group is a Sprite-aware Array; how `for…of` and `[...group]` work on it; why iterating a copy is the safe pattern when removing.
  - **Try it:** spawn 5 sprites into a Group in `setup()`, then in `draw()` log `group.length` and use a `for…of` loop to nudge each sprite's `vel.x` by a small random amount. Student edits the group size and the velocity formula.
- **Topic 2: Group defaults + the factory pattern** — read before `2.3.5 Groups Sandbox`.
  - **What you'll learn from it:** setting `group.color`, `group.diameter`, `group.collider` once applies to every spawn; the `new groupName.Sprite(x, y)` factory inherits defaults; how this saves per-sprite repetition.
  - **Try it:** create a Group, set `color`, `diameter`, `collider`, then spawn 10 sprites in a row using the factory — student edits the defaults and watches every sprite update.
- **Glossary rows from §Vocabulary:** `Group`, `Spawn`, `Despawn`.

#### R2 — Collisions + Overlaps (`2-3-8-reading-collisions`)

- **Topic 1: `sprite.overlaps(other)` — boolean form** — read before `2.3.10 Safe Despawn`.
  - **What you'll learn from it:** `overlaps` returns a boolean per-frame; passing a Group checks against every member; the boolean form is right when you only need a yes/no (e.g. game-over trigger).
  - **Try it:** one player sprite + one Group of 3 enemies; print `player.overlaps(enemies)` to the canvas every frame. Student moves the player around and reads the live boolean.
- **Topic 2: `overlaps(other, callback)` — callback form** — read before `2.3.10 Safe Despawn`.
  - **What you'll learn from it:** the callback fires once per overlapping pair, with both sprites as arguments; this is the clean place to `remove()` the colliding sprite without tripping the iteration bug; ideal for despawn-on-collect / despawn-on-hit patterns.
  - **Try it:** the apple-catcher pattern — one basket sprite + a Group of falling apples; in the callback, increment a score and call `apple.remove()`. Student edits the spawn rate.
- **Glossary rows from §Vocabulary:** `Overlap`, `Iterate backwards`, `Callback`.

## In-App Lessons (carry-overs)

Three pre-existing q5 lessons need migration to fit Unit 2.3's slots and the post-2.2 conventions.

### `lessons/q5play-groups/` → `lessons/2-3-5-groups-sandbox/`

- **New title:** `2.3.5 Groups Sandbox`
- **Slot:** 2.3.5 (q5play lesson, hands-on)
- **Convention violations to fix:**
  - Title prefix `5.3.1 Introducing Groups` → `2.3.5 Groups Sandbox`.
  - `unit: "5.3 Groups & Overlaps"` → `"2.3 Collections and Physics Applications"`.
  - `category: "Unit 5: q5play — Game Physics"` → `"Unit 2: q5play — Applied Game Development"`.
  - `week: 15` → `13`.
  - `script.js` uses `update()` separate from `draw()` — consolidate to `draw()` only (per §Do NOT in this spec). Update breadcrumbs likewise.
  - `passingScore: 20` → set to `totalPoints` per `q5play-lesson-conventions.md` §1 (Submit is all-green-gated).
  - SLOs `["SLO-1", "SLO-4"]` → `["SLO-3", "SLO-4"]` (SLO-1 is lifecycle, not relevant here; SLO-3 is the unit's reinforce target).
- **Data continuity:** renaming the folder changes `lesson.json.id`, which breaks any existing student commits/progress under `q5play-groups`. Acceptable in pre-launch state; flag if students have already enrolled.

### `lessons/q5play-apples/` → `lessons/2-3-7-example-apple-catcher/`

- **New title:** `2.3.7 Worked Example — Apple Catcher`
- **Slot:** 2.3.7 (worked example — read-along, NOT graded)
- **Convention violations to fix:**
  - Type changes from `assignment` to `lesson` and `preview` from `q5play` to `example` per `example-conventions.md` (this is a read-along, not graded — current `lesson.json` mistakenly has the q5 `requirements`-shape).
  - Title prefix `5.3.2 Apple Catcher` → `2.3.7 Worked Example — Apple Catcher`.
  - `unit` / `category` / `week` migration as above.
  - `script.js` uses `update()` + arrow keys — consolidate to `draw()` only and switch to WASD per `lab-assignment-conventions.md` §4.
  - Per `example-conventions.md` §4, this lesson MAY ship a fully-working `script.js` since `grading.totalPoints === 0` — keep the working sketch but author a `content.md` walkthrough (Goal + numbered Step blocks with `js live` fences + Key takeaways) since worked examples primary surface is `content.md`, not the editor.
- **Data continuity:** see above — rename breaks existing `q5play-apples` ids.

### `lessons/q5play-asteroids-challenge/` → `lessons/2-3-11-a13-1-asteroid-field/`

- **New title:** `2.3.11 A13.1 Asteroid Field`
- **Slot:** 2.3.11 (graded q5 assignment — A13.1)
- **Convention violations to fix:**
  - Title prefix `5.3.3 Asteroid Field — Challenge` → `2.3.11 A13.1 Asteroid Field`.
  - `unit` / `category` / `week` migration as above.
  - `script.js` has a `// BUILD THIS:` comment block — per `q5play-challenge-conventions.md` §4 (or `lab-assignment-conventions.md` §1 if treated as a lab), the starter is either an empty file (challenge) or a Steps scaffold with `// STEP N:` breadcrumbs (lab). Since A13.1 is `type: "assignment"` (lab variant per the §Assignments rubric below), use a Steps scaffold matching `lab-assignment-conventions.md`.
  - `points: 50` total — does not match A13.1 spec rubric (20 pts). Reconcile by re-deriving requirements 1:1 against the rubric in §Assignments below.
  - `passingScore: 35` → set to `totalPoints` (20) per `q5play-lesson-conventions.md` §1.
  - `setup()` / `update()` / `draw()` empty placeholders → use `setup()` + `draw()` only (no `update()`) with `// STEP N:` breadcrumbs.
- **Data continuity:** see above — rename breaks existing `q5play-asteroids-challenge` ids.

## Worked Examples (teacher-led)

### Worked Example 1 — Create and iterate a group (10 min)
```js
let enemies;

function setup() {
  new Canvas(400, 400);
  enemies = new Group();
  enemies.color = 'red';

  // Spawn 5 enemies at random positions
  for (let i = 0; i < 5; i++) {
    enemies.push(new Sprite(random(50, 350), random(50, 350), 20, 20));
  }
}

function draw() {
  background('#222');
  // Each enemy wobbles
  for (let e of enemies) {
    e.vel.x = sin(frameCount * 0.1 + e.pos.y) * 0.5;
  }
}
```

### Worked Example 2 — Overlap + scoring (10 min)
```js
let player, apples, score;

function setup() {
  new Canvas(400, 400);
  player = new Sprite(200, 350, 40, 20);
  player.collider = 'kinematic';
  apples = new Group();
  score = 0;
}

function draw() {
  background('#222');

  if (kb.pressing('left'))  player.vel.x = -5;
  else if (kb.pressing('right')) player.vel.x = 5;
  else player.vel.x = 0;

  // Spawn an apple every 30 frames
  if (frameCount % 30 === 0) {
    apples.push(new Sprite(random(20, 380), 0, 20, 20));
  }

  // Check overlaps with callback
  player.overlaps(apples, (p, apple) => {
    score++;
    apple.remove();
  });

  // Despawn apples that fell off-screen — iterate BACKWARDS
  for (let i = apples.length - 1; i >= 0; i--) {
    if (apples[i].pos.y > 410) apples[i].remove();
  }

  text(`Score: ${score}`, 10, 20);
}
```

Point out: the `overlaps` callback handles despawn cleanly; the manual backwards loop is for "off-screen" cleanup.

### Worked Example 3 — The remove-during-iteration bug (5 min, projector)
Demonstrate the BUG intentionally:
```js
// BAD — don't do this
for (let i = 0; i < apples.length; i++) {
  if (apples[i].pos.y > 410) apples[i].remove();  // skips the next apple after each removal
}
```
Show students the visual: apples appear to "skip" getting removed. Explain index shift. Then fix:
```js
// GOOD — iterate backwards
for (let i = apples.length - 1; i >= 0; i--) {
  if (apples[i].pos.y > 410) apples[i].remove();
}
```

## Challenges (optional stretch)

- **Challenge 1:** Add a "lives" counter. Game over when lives = 0.
- **Challenge 2:** Spawn apples with varying sizes and colors; bigger apples = more points.
- **Challenge 3:** Implement `enemies.cull()` — a method that removes any enemy past the canvas edge. Compare to the manual backwards loop.

## Session Plan

### Session 1 (~105 min) — Groups intro + Apple Catcher

| Time | Activity | Notes |
|------|----------|-------|
| 0:00 | V1 — Groups video | |
| 0:05 | Worked Example 1 — create + iterate | 10 min |
| 0:15 | In-app lesson `q5play-groups` | 25 min hands-on |
| 0:40 | V2 — overlaps() video | |
| 0:45 | Worked Example 2 — overlap + scoring | 15 min |
| 1:00 | In-app worked example `q5play-apples` (read-along) | 20 min |
| 1:20 | R1 — Groups docs reading | 15 min |
| 1:35 | Hand out A13.1 Asteroid Field challenge | Due end of week |

### Session 2 (~105 min) — Asteroid Field build + overlap deep dive

| Time | Activity | Notes |
|------|----------|-------|
| 0:00 | V3 — remove-during-iteration bug video | |
| 0:05 | Worked Example 3 — the bug live | 10 min |
| 0:15 | A13.1 Asteroid Field build time | 75 min |
| 1:30 | Share-out: 3 student asteroid demos | 10 min |
| 1:40 | Preview W14 — physics applications | |

---

## Assignments

### A13.1 — Asteroid Field Challenge
**SLO 3 primary lab + SLO 4 (collision algorithm)**

| | |
|---|---|
| Type | Lab (in-app editor — `q5play-asteroids-challenge` lesson) |
| Points | 20 |
| Est. time | 90 min |
| Due | End of Week 13 |

**Task:** Complete the Asteroid Field challenge fully.

**Requirements:**
- Spawn **at least 10 asteroids** at random positions (can be initial spawn or timed via `frameCount`).
- Asteroids drift in some direction (velocity varies by asteroid).
- A **player ship** that moves via arrow or WASD keys.
- **Detect overlap** between player and any asteroid; on hit, transition to a "hit" state (log a message, change background color, or move to a game-over screen).
- **Despawn asteroids that leave the canvas** using the safe backwards-iteration pattern.
- Optionally: lives counter, score, restart key.

**Rubric (20 pts):**
| Criterion | Pts |
|-----------|-----|
| 10+ asteroids spawned | 3 |
| Asteroids have varied velocity | 2 |
| Player responds to input | 3 |
| Overlap with player correctly detected | 4 |
| Hit transition works (state change or clear feedback) | 3 |
| Safe despawn loop (backwards iteration) | 3 |
| Code follows style guide | 2 |

Students complete the in-app `q5play-asteroids-challenge` lesson for auto-graded requirements, then submit a screenshot of the running game demonstrating an overlap triggering the hit state.

---

## Vocabulary

| Term | Definition |
|------|-----------|
| **Group** | A q5play collection that behaves like an array of sprites with shared defaults |
| **Overlap** | When two sprites' bounding boxes intersect |
| **Spawn** | Create a new sprite during gameplay |
| **Despawn** | Remove a sprite during gameplay |
| **Iterate backwards** | Loop from `length - 1` down to `0` to safely remove items |
| **Callback** | A function passed as an argument to another function (e.g., the overlap handler) |

---

## Teacher Notes

- The remove-during-iteration bug will catch 80% of students. Front-load the fix.
- `overlaps(other, callback)` is cleaner than manual overlap loops. Lead with the callback form.
- Groups have shared defaults (set `.color` on the Group to color all members). Mention as a time-saver.
- This module reinforces Q1 W8 arrays — students who struggled with arrays in Q1 get a second shot in a motivating context.

---

## Numbered Lesson List

This sub-module fills slots `2.3.1` through `2.3.12` of unit 2.3. (Sub-module 2.3.2 picks up at `2.3.13`.) Per `sub-module-spec-conventions.md` §3.3, every lesson below is created in `lessons/<slug>/` with `lesson.json` matching the per-type convention referenced in the **Type** column.

| Slot | Type | Title | Slug | Source / artifact | Notes |
|------|------|-------|------|-------------------|-------|
| 2.3.1 | slides | `2.3.1 Slides — Collections and Physics Applications` | `2-3-1-slides` | Unit 2.3 slide deck (`slides/2.3/slides.md`, shared with 2.3.2) | Per `slide-deck-conventions.md`. Lesson is metadata-only until the deck ships; `slidesUrl: "/slides/2.3/"`. |
| 2.3.2 | video | `2.3.2 Video — Groups in q5play` | `2-3-2-video-groups-intro` | V1 above | `videoUrl` empty per `video-conventions.md` §3. |
| 2.3.3 | reading | `2.3.3 Reading — q5play docs: Groups` | `2-3-3-reading-groups` | R1 + R1 reading guidance above | Two `## <Topic>` subsections + glossary per `reading-conventions.md` §3. |
| 2.3.4 | example | `2.3.4 Worked Example — Iterating a Group` | `2-3-4-example-iterate-group` | Worked Example 1 (above) | One `## Step` per code-walkthrough beat per `example-conventions.md` §3. |
| 2.3.5 | q5play (lesson) | `2.3.5 Groups Sandbox` | `2-3-5-groups-sandbox` | **Migration** of `lessons/q5play-groups/` (see §In-App Lessons) | Hands-on graded q5 lesson — Steps + Requirements per `q5play-lesson-conventions.md`. |
| 2.3.6 | video | `2.3.6 Video — overlaps() boolean vs callback` | `2-3-6-video-overlaps` | V2 above | |
| 2.3.7 | example | `2.3.7 Worked Example — Apple Catcher` | `2-3-7-example-apple-catcher` | Worked Example 2 (above) + **migration** of `lessons/q5play-apples/` | Per `example-conventions.md` §4 the working `script.js` may stay; primary surface is the `content.md` walkthrough. |
| 2.3.8 | reading | `2.3.8 Reading — q5play docs: Collisions + Overlaps` | `2-3-8-reading-collisions` | R2 + R2 reading guidance above | |
| 2.3.9 | video | `2.3.9 Video — The remove-during-iteration bug` | `2-3-9-video-remove-bug` | V3 above | |
| 2.3.10 | example | `2.3.10 Worked Example — Safe Despawn` | `2-3-10-example-safe-despawn` | Worked Example 3 (above) | Demonstrates the bug live, then the iterate-backwards / iterate-a-copy fix. |
| 2.3.11 | q5play (assignment) | `2.3.11 A13.1 Asteroid Field` | `2-3-11-a13-1-asteroid-field` | A13.1 (§Assignments below) + **migration** of `lessons/q5play-asteroids-challenge/` | `type: "assignment"`. Steps + Requirements derived 1:1 from the A13.1 rubric. `totalPoints` = 20. |
| 2.3.12 | q5play (challenge) | `2.3.12 Challenges — Optional Stretch` | `2-3-12-challenges` | §Challenges (above) | `type: "challenge"`, empty `script.js`, `content.md` lists the three stretch ideas (lives counter, varied apples, `cull()` method) per `q5play-challenge-conventions.md` §3. |

### Do NOT (for the build)
- Do not introduce `update()` as a separate function — every q5 starter for this sub-module uses `draw()` only (per §Do NOT in Context).
- Do not author additional in-app challenges beyond the §Challenges list — the optional-stretch lesson at slot 2.3.12 is the one place those live.
- Do not invent video URLs — V1/V2/V3 `videoUrl` cells stay empty per `video-conventions.md` §3.
- Do not preserve the legacy slugs (`q5play-groups` etc.) — rename per the slug rule (`sub-module-spec-conventions.md` §4) so the `<U>-<M>-<L>` triple is meaningful.

## Status
- `draft`
