---
id: "1.1"
title: Foundations
quarter: 1
weeks: [1]
environment: console
status: draft
---

# Module 1.1 — Foundations

**Quarter 1 · Week 1 · Environment: browser DevTools console**

## SLO Focus
- **Introduces SLO-1** (software life-cycle) — primary artifact A1.1.
- Reinforces **SLO-4** (process as algorithm).

## Module Goal
Students understand what programming is and where it sits in a software
project's lifecycle: the four framework activities, the umbrella activities
that run alongside them, task sets, SDLC methodologies and the four Ps, and
how to judge whether a prescriptive process model fits a given project.

## Pre-requisite snapshot
None. First module of the course.

## Post-requisite snapshot
Can name the four framework activities and classify a task into the correct
one; can name umbrella activities; can write a task set; can explain SDLC
methodologies and the four Ps; can judge whether a prescriptive process
model fits a given project; can open the browser console and run
`console.log`.

## Note
Book §1.1 Software Lifecycle. Ships as a 23-lesson sequence (`1-1-1-slides`
… `1-1-23-a1-2-describe-lifecycle`), renumbered into strict sequence on
2026-08-12 (letter-suffix inserts retired — see `BOOK-TO-MODULE.md`). Full
Numbered Lesson List has not yet been spec'd out per
`sub-module-spec-conventions.md`.

Points audit (2026-08-13): only `1.1.4` (`sdlc-overview`, a console/regex lab)
was a genuine no-points violation — zeroed. `1.1.16`, `1.1.19`, `1.1.22`, and
`1.1.23` are AI-graded written assignments correctly carrying nonzero
`aiGrader.rubric[].points` per `written-assignment-conventions.md`'s
points-weighted rubric shape — not a bug. Note that written assignments
(unlike this module's 3 console labs) are **not** gated at 100%:
`components/WrittenGrader.tsx` allows partial credit (≥70% of rubric points
for the `1`-per-item shape used here; `1-1-3-a1-1-sdlc-writeup` and
`1-1-7-a3-3-unit-quiz` use the `0`-per-item shape instead, gated at a bare
majority of criteria). See `written-assignment-conventions.md` §4.
