---
id: "5.1"
title: Hello Sprite and Movement
quarter: 2
weeks: [10, 11]
environment: moSHion
status: shipped
---

# Module 5.1: Hello Sprite and Movement

**Quarter 2 · ~1.75 contact hours (1 class meeting)**
**Environment:** moSHion in-app editor (built on q5.js + Box2D physics).

## SLO Focus
- Reinforces **SLO 3** (design/implement/test in a visual context)
- Applies Q1 control structures (conditionals, loops) to interactive programs: first time students use their language skills for something that moves on screen

## Module Goal
Students can build a minimum-viable moSHion sketch from scratch: a canvas, a controllable sprite, keyboard input, and basic movement. They understand the frame loop (`setup()` runs once, `draw()` runs every frame) and can predict what a `vel.x = 4` assignment does.

## Pre-requisite snapshot
- Completed Q1 (variables, types, conditionals, loops, functions, arrays).
- Comfortable reading and writing ~30-line console.log programs.
- Can follow camelCase + class style guide.

## Post-requisite snapshot
- Can write a moSHion sketch with canvas, sprite, keyboard input, and basic physics from memory.
- Uses `kb.pressing(...)` correctly including the else-to-zero pattern.
- Understands that `vel` is pixels-per-frame at ~60 fps.
- Has instantiated at least 3 objects without being told they're using OOP: deliberate setup for the §5.3 reveal.

## Note
Book §5.1 Hello Sprite and Movement. Currently **23 lessons** (`5.1.1`–`5.1.23`),
folders `lessons/5-1-*`. No folder/title numbering mismatch: this module was built
cleanly after the book-native renumber, unlike §5.3/§5.4 next door (see
`5.4_writing-your-own-classes.md`'s Note).

## Unit-level assessments
- **A5.1.1 (Lab, `5-1-21-a10-1-sprite-playground`):** sprite playground — canvas, one WASD-controlled sprite, a second auto-moving sprite (`frameCount`), an on-screen text label.
- **A5.1.2 (Written, `5-1-22-a10-2-frame-loop`):** half-page explanation of `setup()` vs `draw()` and what "60 frames per second" means for the values students pick.
- Both slugs still carry their pre-renumber artifact ids (`a10-1`/`a10-2`) rather than `a5-1-1`/`a5-1-2`: folder names are frozen once shipped (renaming orphans student progress), so the mismatch between slug and current `A5.1.x` code is cosmetic and left as-is.

No unit-closing quiz for this module: Midterm 3 covers Units 5.1–5.3/5.4 together at the end of Q2's moSHion block.
