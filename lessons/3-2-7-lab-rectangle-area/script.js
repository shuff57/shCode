// 3.2.7 findRectangleArea

// STEP 1: Define a function called findRectangleArea that takes two
//         parameters: width and height.

// STEP 2: Inside the function, hand back the area.
//         Multiply width by height and return the result.
//         Use return, not console.log, so the caller receives a number.

// STEP 3: Call findRectangleArea with two numbers.
//         Store the returned value or pass it straight into console.log,
//         then log the result. Make a second call with different numbers.
