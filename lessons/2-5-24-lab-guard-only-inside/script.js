// 2.5.24 Guard Only What's Inside

// STEP 1: Write a try block that logs firstMissing (never declared).
//         In the catch, log "Caught the first one."

// STEP 2: After the whole try...catch, log secondMissing (also never
//         declared) — with no try around it.

// STEP 3: Run it. The first error is caught; the second is not. In a
//         comment, explain why.
