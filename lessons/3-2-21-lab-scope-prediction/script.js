// 3.2.21 Scope Prediction Exercise

// STEP 1: Outside any function, declare a variable called score with let
//         and set it to a number. Being outside every function makes it global,
//         so every function can read it.

// STEP 2: Define a function called showScore that logs score.
//         Because score is global, the function can read it.

// STEP 3: Define a function called describeScore.
//         Inside it, declare a variable called label with let, then log
//         score and label together. The label lives only while the function runs.

// STEP 4: Call both functions so the working cases run.
//         Then, after the calls, write a console.log of a variable that only
//         existed inside a function. Predict the output in a comment first:
//         a local variable cannot be reached from outside its function.
