// 3.2.4c Lab — Add a Torus
let ring;

function setup() {
  // STEP 1: Create ring = new Torus(0, 0, 0, 1.5, 0.3).
  //         The 4th argument is ringRadius; the 5th is tubeRadius.
  //         Use two different values so the difference is visible.

  // STEP 2: Set ring.color to any CSS color.

}

function draw() {
  background('#111');
}
