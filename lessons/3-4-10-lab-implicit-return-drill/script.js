// 3.4.10 Implicit-Return Rewrite Drill

// STEP 1: Create three const variables. Assign each one a single-expression
//         arrow function with a parameter and NO braces and NO return
//         keyword. Give them three different jobs, for example:
//         - one that multiplies its input,
//         - one that answers a yes/no question about its input,
//         - one that joins two string inputs with a space.

// STEP 2: Keep every body as a bare expression directly after the arrow.
//         If you add braces, the implied return stops working.

// STEP 3: Call each function with sample arguments and print each result
//         with console.log. Check each answer against what you expect.
