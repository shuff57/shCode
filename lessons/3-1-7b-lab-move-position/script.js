// 3.1.7b Lab — Move a Cube along the X axis each frame.
let cube;

function setup() {
  background('#111');
  // STEP 1: Create a Cube at (0, 0, 0) and store it in cube.

}

function draw() {
  background('#111');
  // STEP 2: Add 0.05 to cube.position.x so it moves right each frame.

}
