// 2.1.11 Nest a Check

// STEP 1: Create isLoggedIn and isAdmin, both true or false.

// STEP 2: Write an outer if (isLoggedIn) block. Inside it, nest an
//         if / else that checks isAdmin:
//           true  -> log "Welcome, admin."
//           false -> log "Welcome, user."
//         Add an outer else that logs "Please log in."
