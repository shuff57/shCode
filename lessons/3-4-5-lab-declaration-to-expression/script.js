// 3.4.5 Rewrite a Declaration as an Expression

// STEP 1: Create a const and assign it an unnamed function expression.
//         Use the `function` keyword with NO name after it, then the
//         parameter list and a body that returns a value computed from
//         that parameter. Example shape (choose your own): a function
//         that turns a number into a different number.

// STEP 2: Call your function through the const with a sample argument,
//         and wrap that call in console.log to print the result.

// STEP 3: Re-read your line from STEP 1. Confirm there is no name between
//         `function` and `(`, and that the assignment ends with a semicolon.
