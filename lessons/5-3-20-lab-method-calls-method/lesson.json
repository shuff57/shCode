{
  "id": "5-3-20-lab-method-calls-method",
  "title": "5.4.5 Lab — A method calling `this.otherMethod()`",
  "description": "Write `Counter.bigStep()` which calls `this.addBy(5)` and returns `this.isHigh()` — one method dispatching another on the same instance.",
  "type": "lesson",
  "difficulty": "beginner",
  "estimateMins": 8,
  "category": "Unit 2: shplay — Applied Game Development",
  "unit": "5.4 Writing Your Own Classes",
  "preview": "shplay",
  "week": 12,
  "slos": [
    "SLO-2",
    "SLO-3"
  ],
  "steps": [
    {
      "id": "s1",
      "title": "Write the bigStep() method",
      "instructions": "Inside the `Counter` class body, write a `bigStep()` method that calls `this.addBy(5)` and then returns `this.isHigh()`. Press B in the sketch to call it. The counter jumps by 5 each press and turns red once it passes 10.",
      "hints": [
        "Open the shplay docs drawer on the right — a method can call other methods on the same instance using `this.methodName()`."
      ]
    }
  ],
  "requirements": [
    {
      "id": "r1",
      "title": "bigStep() calls this.addBy(5) and returns this.isHigh()",
      "description": "Write `bigStep()` inside `Counter`. It must call `this.addBy(5)` and return `this.isHigh()`.",
      "type": "regex",
      "file": "script.js",
      "pattern": "bigStep[\\s\\S]*this\\.addBy\\s*\\(\\s*5\\s*\\)[\\s\\S]*return\\s+this\\.isHigh\\s*\\(\\s*\\)",
      "flags": "",
      "points": 0,
      "status": "pending"
    }
  ],
  "grading": {
    "totalPoints": 0,
    "passingScore": 0,
    "allowLateSubmit": true
  }
}
