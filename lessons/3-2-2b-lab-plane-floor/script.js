// 3.2.2b Lab — Plane as floor
let floor, sphere;

function setup() {
  // STEP 1: Create floor = new Plane(0, -1, 0, 10, 10) for a 10x10 floor at y = -1.

  // STEP 2: Set floor.color to a dark color like '#444'.

  // STEP 3: Create a Sphere at (0, 0.5, 0) with radius 0.7 and give it a color.

}

function draw() {
  background('#111');
}
