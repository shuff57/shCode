// 3.3.14 Lab: Write a Function That Filters an Array

// STEP 1: Write a function declaration (your own name, e.g. doubled
//         or bigger) that takes an array parameter.

// STEP 2: Inside the function, create a new empty array. Use a
//         for...of loop that pushes each transformed value into it.

// STEP 3: After the loop, return the new array.

// STEP 4: Call the function with a sample array and console.log both
//         the returned result AND the original array, to show the
//         original was not changed.
