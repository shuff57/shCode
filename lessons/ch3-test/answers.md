# Chapter 3 Test — Written Section

Closed book. Answer in your own words, in complete sentences.
One good sentence beats three vague ones.

Name: ________

---

**Q1. (§3.2)** What is the difference between a *parameter* and an *argument*?
Point at one of each in the code you wrote on this test.

A1:

---

**Q2. (§3.6)** You pass an array into a function, and the function changes one of
the values inside it. Does the array held by the *caller* change too? Explain
why, using the word *reference*. Then say what would have been different if the
function had **reassigned** the parameter instead of changing what is inside it.

A2:

---

**Q3. (§3.2)** What value does a function hand back when it has no `return`
statement, and what happens if you store that value and try to do math with it?
A classmate says their function "works" because it prints the answer with
`console.log`. Why is that not the same as returning it?

A3:

---

**Q4. (§3.7)** `.slice()` and `.push()` both work on arrays, but only one of them
changes the array it is called on. Say which is which, using the terms
*non-mutating* and *mutating*. Then explain why a function that returns
something new is easier to trust than one that changes what it was handed.

A4:
