const triple = (n) => n * 3;
const isNegative = (n) => n < 0;
const label = (name, score) => name + ": " + score;

console.log(triple(5));
console.log(isNegative(-1));
console.log(label("Ada", 100));
