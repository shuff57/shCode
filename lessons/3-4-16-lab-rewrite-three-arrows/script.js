// 3.4.16 Rewrite 3 Named Functions as Arrows

// STEP 1: Define three separate function declarations with the
//         `function name() {}` form. Give each a different job:
//         - one that returns a number computed from its input,
//         - one that returns a true/false answer,
//         - one that returns a string built from its inputs.

// STEP 2: Rewrite each declaration as a const assigned a one-line arrow
//         function using implicit return. Keep the same parameters and
//         the same returned value as the declaration you replaced.

// STEP 3: Call each arrow with sample arguments and log the results,
//         checking each one is what the original declaration returned.
