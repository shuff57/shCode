// 2.1.42 A2.1.1 -- Grade Recommendation Advisor

// STEP 1: Create score, attendance, and lateAssignments.
//         Give each a hardcoded number value.

// STEP 2: Write an else if chain (at least two else if branches) that
//         picks a grade recommendation string based on score.

// STEP 3: Add a condition using && or || that combines attendance and
//         lateAssignments (for example: attendance below 90 OR
//         lateAssignments 3 or more) and logs a review warning when true.

// STEP 4: Print the grade recommendation, and the review warning if it applies.
