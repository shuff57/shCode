// 1.2.6 Add an else

// STEP 1: Create a variable called score and assign it a number value.
//         Then write an if statement that checks whether score >= 70.
//         Inside the if block, log a message that the score passes.

// STEP 2: Right after the closing brace } of the if block, add an else block.
//         Inside the else block, log a message that the score does not pass.
