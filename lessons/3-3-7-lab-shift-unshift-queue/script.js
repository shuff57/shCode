// 3.3.7 Lab: Front-of-Line Queue

// STEP 1: Create a variable called line and assign it an array
//         containing at least two name strings.
//         Example shape (choose your own names): ["ana", "bob"]

// STEP 2: Use .push() to add one more name onto the back of the line.

// STEP 3: Use .shift() to remove the first person from the line.
//         Store the returned value in a variable (it is the person
//         now being served).

// STEP 4: console.log who is being served, then console.log the
//         remaining line array.
