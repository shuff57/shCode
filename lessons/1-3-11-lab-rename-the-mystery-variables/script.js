// 1.3.11 Rename the Mystery Variables

// Two working snippets. Both are hard to read for exactly one reason.
// Rename every variable. Do not change what the code does.
// When you are finished, no single letter should be left.

// --- Snippet 1: a grade report ---
// rename to: subjectName, finalGrade, gradeReport
let a = "Math";
let b = 88;
let c = a + " final grade: " + b + "%";
console.log(c);

// --- Snippet 2: a pay calculation ---
// rename to: hoursWorked, hourlyRate, totalPay
let n = 3;
let d = 2.5;
let p = n * d;
console.log(p);
