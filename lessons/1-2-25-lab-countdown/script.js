// 1.2.25 Countdown with while

// STEP 1: Create a variable called count and set it to 10.

// STEP 2: Write a while loop.
//         The condition should keep the loop running as long as count > 0.
//         Inside the loop:
//           - Use console.log to print the current value of count.
//           - Decrease count by 1 so the loop eventually stops.
//         When you run the code you should see 10, 9, 8 ... 1 in the console.
