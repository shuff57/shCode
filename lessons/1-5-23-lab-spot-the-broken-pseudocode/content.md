Write your response in the box below. You can revise and resubmit as many times as you like.

**Pseudocode, not JavaScript.** You have not been taught `while` as code yet — that arrives in §2.2. This lesson is about catching a broken *plan* before any code exists, which is exactly the skill §1.5.4 said pseudocode is for: nothing checks it, so a human has to.

Read the plan aloud, one line at a time, as if you were the machine. The flaw is findable in about ten seconds that way, and nearly invisible if you skim.

---

## Rubric

| # | Criterion |
|---|-----------|
| 1 | Q1: identifies that `count` never changes, so the plan never finishes |
| 2 | Q2: the rewritten plan adds the missing step |
| 3 | Q2: indentation shows which steps are inside the loop |
| 4 | Q2: the answer is pseudocode, not JavaScript |
| 5 | Q3: names the line that guarantees termination, and why it does |
| 6 | Q4: says why reading this plan beat running it |
