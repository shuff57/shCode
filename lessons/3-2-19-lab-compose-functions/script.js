// 3.2.19 Compose Two Functions

// STEP 1: Define a function called double that takes one parameter n
//         and returns n multiplied by 2.

// STEP 2: Define a function called addTen that takes one parameter n
//         and returns n plus 10.

// STEP 3: Compose them. Call one function with the result of the other,
//         for example addTen(double(5)). Both must return values so the
//         inner result can become the outer argument.

// STEP 4: Log the composed result, then compose them in the opposite order
//         and log that too. The answers should differ.
