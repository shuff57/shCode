// 3.5.25 Objects Capstone: Build, Read, Destructure

// STEP 1: Create one array whose elements are objects. Give every object the
//         same fields (a name, a price, a quantity, whatever you like), and
//         include at least three records.

// STEP 2: Give one record a nested object as a field, and give one record a
//         key that contains a space, written in quotes.

// STEP 3: Print one field with a dot, the spaced key with square brackets,
//         and the nested field with a chain of two dots.

// STEP 4: Loop over the array and print each record's name and its cost
//         (price times quantity), reading each field off the record with a dot.

// STEP 5: Destructure two or more fields out of one record into variables in
//         one statement, then use the bare names in a printed sentence.

// STEP 6: Use Object.keys with a loop to print every key and value of one
//         record. Inside the loop the key lives in a variable, so fetch each
//         value with square brackets.
