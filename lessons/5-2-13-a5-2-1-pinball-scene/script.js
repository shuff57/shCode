// 5.2.13 A5.2.1 Pinball Scene: gravity, obstacles, a bouncy ball, a reset key.

let ball;

function setup() {
  new Canvas(400, 400);

  // STEP 1: Turn on gravity so the ball falls.

  // STEP 2: Build at least three static obstacle sprites: bumpers, ramps,
  //         or walls the ball can bounce off of. Give each one a shape,
  //         position, and color.

  // STEP 3: Create the ball as a dynamic sprite. Set its bounciness above
  //         0.8 so it feels lively, not sticky.
}

function draw() {
  background('#222');

  // STEP 4: Add a reset key. When pressed, move the ball back to a
  //         starting position near the top of the canvas (set its x
  //         and/or y directly).
}
