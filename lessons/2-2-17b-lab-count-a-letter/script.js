// 2.2.20 Count a Letter in a Word

let word = "strawberry";
let count = 0;

// STEP 1: Write a for loop that runs i from 0 while i < word.length.

// STEP 2: Inside the loop, use an if to check whether word[i] === "r".

// STEP 3: When it matches, add 1 to count.

// STEP 4: After the loop, log count.
//         "strawberry" has three r's, so you should see 3.
