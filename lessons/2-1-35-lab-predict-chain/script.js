// 2.1.39 Predict the Chain

// STEP 1: Write an else if chain of your own (at least 3 branches,
//         using a variable and comparisons of your choice).
//         Before running, write a comment above it guessing which
//         branch will fire. Run it and check your prediction.

// STEP 2: Write a second chain using ? operators instead of else if,
//         with different cutoff values than STEP 1.
//         Predict the result in a comment first, then run it.
