// 3.5.12 Build a Small Nested-Object Structure

// STEP 1: Create a variable named order and give it an object literal.

// STEP 2: Add a customer property whose value is another object literal
//         with at least a name field inside it.

// STEP 3: Add an items property whose value is an array of at least two
//         objects, each holding a name and a price.

// STEP 4: Print the customer's name by walking the chain one step at a time:
//         order, a dot, customer, a dot, name.

// STEP 5: Loop over the items array. Pick one item by its index, then read
//         each field of that item with a dot, and print the name and price.
