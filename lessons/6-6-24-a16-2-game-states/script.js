// 2.6.24 A16.2 Game States: State machine with persistence

let state;

function setup() {
  // STEP 1: Create canvas, initialize state to 'title', initialize game variables

}

function draw() {
  background('#222');

  // STEP 2: switch(state) with cases for 'title', 'play', 'pause', 'gameover'

  //   case 'title':   Show game title, instructions, New Game / Continue options
  //   case 'play':    Player movement, scoring, condition checks for gameover
  //   case 'pause':   Show paused overlay, wait for P to unpause
  //   case 'gameover':Show final score, auto-save, restart option

}
