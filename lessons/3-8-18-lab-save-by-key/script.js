// 3.8.18 Save a State Object by Key

// STEP 1: Create a variable called state holding an object with at least two
//         properties, for example a score and a level.

// STEP 2: Use localStorage.setItem to save the object under a key you choose.
//         Remember the store holds strings, so the object has to be converted
//         with JSON.stringify first.

// STEP 3: Use console.log with localStorage.getItem on the same key to confirm
//         text was stored.
