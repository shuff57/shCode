// 3.7.21 Mutating vs. Non-Mutating Method Sort Drill

// STEP 1: Create a variable holding an array of at least three values.
//         You will demonstrate two different methods on it.

// STEP 2: Call a method that changes the array IN PLACE (for example .push()).
//         Log the array before and after so the change is visible.

// STEP 3: Call a method that returns a NEW array and leaves the original
//         alone (for example .map() or .slice()). Store the result in a new
//         variable.

// STEP 4: Use console.log to print the original array after the non-mutating
//         call, so you can see it did not change.
