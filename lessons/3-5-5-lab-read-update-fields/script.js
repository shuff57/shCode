// 3.5.5 Read and Update Object Fields

// STEP 1: Create a variable named book and give it an object literal
//         holding at least two properties, such as a title and a page count.

// STEP 2: Print one of those properties. Reach it with a dot: the object
//         name, a dot, then the property name.

// STEP 3: Assign a new value to one of the properties that already exists.

// STEP 4: Add a property the object did not have before, by assigning to it:
//         object name, a dot, the new name.

// STEP 5: Print the whole object so the changed and added fields are visible.
