// 2.5.9 What Runs, What Doesn't

// STEP 1: In a comment, predict which lines print: "A", boom, "B", "C", "D".

// STEP 2: Write a try block that logs "A", then logs boom (never
//         declared anywhere in this file), then logs "B".
//         In the catch block, log "C".
//         After the whole try...catch, log "D".

// STEP 3: Run it. Compare what actually printed to your prediction.
