// 2.2.6 Your Turn — Build a sprite gallery of your own.
//
// You've watched new Sprite(...) stamp out instances in 2.2.4 and 2.2.5.
// Now write the code yourself. Follow the STEP comments below.

function setup() {
  // STEP 1: create a 400x400 canvas with new Canvas(...)


  // STEP 2: instantiate a rectangle sprite with the 4-arg form
  //   new Sprite(x, y, width, height)
  // Store it in a variable and set its .color.


  // STEP 3: instantiate a circle sprite with the 3-arg form
  //   new Sprite(x, y, diameter)
  // Store it in a variable and set its .color.


  // STEP 4: pick one sprite and give it a .stroke and a .strokeWeight.


}

function draw() {
  background('#222');
  // Sprites render automatically every frame — no draw code needed for this lab.
}
