// 3.1.7e Lab — Use radians() to set a fixed rotation angle.
let cube;

function setup() {
  background('#111');
  // STEP 1: Create a Cube at (0, 0, 0) and store it in cube.

  // STEP 2: Set cube.rotation.y to radians(45) — a 45-degree tilt.

}

function draw() {
  background('#111');
}
