// 2.1.37 Combine &&, ||, and !

// STEP 1: Create hasTicket (true/false), age (a number),
//         and isBlocked (true/false).

// STEP 2: Write one if condition that uses all three:
//           && to require hasTicket
//           || so age < 5 or age >= 65 also counts as eligible
//           !  to require isBlocked to be false
//         Example shape: (hasTicket && (age < 5 || age >= 65)) && !isBlocked
//         Log a message for true, and an else message for false.
