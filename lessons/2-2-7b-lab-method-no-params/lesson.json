{
  "id": "2-2-7b-lab-method-no-params",
  "title": "2.2.7b Lab — Method with no params",
  "description": "Write `Counter.tick()` — a method that takes no arguments and mutates `this.n`. First method you write from scratch.",
  "type": "lesson",
  "difficulty": "beginner",
  "estimateMins": 8,
  "category": "Unit 2: shplay — Applied Game Development",
  "unit": "2.2 Object-Oriented Programming",
  "preview": "shplay",
  "week": 12,
  "slos": ["SLO-2", "SLO-3"],
  "steps": [
    {
      "id": "s1",
      "title": "Write the tick() method",
      "instructions": "Inside the `Counter` class body, after the constructor, write a `tick()` method that adds 1 to `this.n`. Press space in the sketch to call it and watch the number on screen increase.",
      "hints": ["Open the shplay docs drawer on the right and review the class syntax section for method declaration format."]
    }
  ],
  "requirements": [
    {
      "id": "r1",
      "title": "tick() method increments this.n",
      "description": "Write a `tick()` method inside the `Counter` class that adds 1 to `this.n` using `+=` or `++`.",
      "type": "regex",
      "file": "script.js",
      "pattern": "tick\\s*\\(\\s*\\)\\s*\\{[\\s\\S]*?this\\.n\\s*(?:\\+=|=\\s*this\\.n\\s*\\+|\\+\\+)",
      "flags": "s",
      "points": 0,
      "status": "pending"
    }
  ],
  "grading": { "totalPoints": 0, "passingScore": 0, "allowLateSubmit": true }
}
