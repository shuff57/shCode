// 3.2.B1 Build-Up — Create the Sun — solution
let sun;

function setup() {
  background('#000');
  sun = new Sphere(0, 0, 0, 1.2);
  sun.color = 'yellow';
}

function draw() {
  background('#000');
}
