// 3.8.10 Round-Trip an Object Through JSON

// STEP 1: Create a variable holding an object with at least two properties,
//         one of them a number.

// STEP 2: Use JSON.stringify to turn the object into a string, and store that
//         string in a second variable.

// STEP 3: Use JSON.parse on the string to build a third variable back into a
//         usable object.

// STEP 4: Use console.log to print a number property of the restored object
//         plus one, and print one of the original properties. Confirm the
//         restored value behaves like a real object.
