// 3.7.11 .concat() Two Lists

// STEP 1: Create two variables, each holding an array of at least three
//         values. Choose your own two lists (for example, two course names).

// STEP 2: Use .concat() on the first array to join the second array onto the
//         end, and store the result in a third variable. Do not change either
//         original.

// STEP 3: Use console.log to print the joined array and both originals.
//         Check that neither original array changed.
