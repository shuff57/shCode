// 2.1.32 Branch with ||

// STEP 1: Create two variables.
//         For example, isWeekend and isHoliday (true/false),
//         or two numbers you can compare.
//         Give each variable a value.

// STEP 2: Write an if statement whose condition uses || to combine two checks.
//         The if block should run if at least one of the checks is true.
//         Inside the if block, use console.log to print a message
//         saying that at least one condition is true.
