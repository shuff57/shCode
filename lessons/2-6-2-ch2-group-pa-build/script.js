// Chapter 2 Group PA, Part 2 of 3: Build It.
// Problem:
// Partners:
// Date:

// STEP 1: Declare your inputs and your limit.
// One variable per number, one for the name or label, and the fixed
// limit as a const (2.6.1 asked you to pick them).

// STEP 2: Copy the chart from 2.6.1 in as comments.
// One chart shape per // line, in order, indented to show what sits
// inside the loop and inside the decision.

// STEP 3: Build the program under the chart it came from.
// Loop, continue, the work, the break, the switch, the guard -- in
// the order the chart says. One screen, partners swap the keyboard
// at least once.