// 3.3.17 Lab: Read and Update a Nested Array

// STEP 1: Create a variable called grid and assign it a 2x2 (or
//         larger) nested array of numbers.
//         Example shape (choose your own values): [[1, 2], [3, 4]]

// STEP 2: Use double-index syntax (grid[r][c]) to console.log one
//         specific inner value.

// STEP 3: Reassign one inner value with grid[r][c] = ... .

// STEP 4: Use a nested loop (outer over rows, inner over cells) to
//         add up every number in the grid, then console.log the total.
