// Prediction: multiplication happens before addition, so this is 2 + (2*3) = 8
console.log(2 + 2 * 3);

// Prediction: the brackets change the order, so this is (2+2) * 3 = 12
console.log((2 + 2) * 3);

// The rule: multiplication happens before addition, unless brackets change the order of operations.

console.log(typeof 2);
