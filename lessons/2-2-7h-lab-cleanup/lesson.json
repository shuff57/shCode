{
  "id": "2-2-7h-lab-cleanup",
  "title": "2.2.7h Lab — Cleanup with `this.sprite.delete()`",
  "description": "Write `Bubble.pop()` which calls `this.sprite.delete()` so the object removes its own sprite on click.",
  "type": "lesson",
  "difficulty": "beginner",
  "estimateMins": 6,
  "category": "Unit 2: shplay — Applied Game Development",
  "unit": "2.2 Object-Oriented Programming",
  "preview": "shplay",
  "week": 12,
  "slos": ["SLO-2", "SLO-3"],
  "steps": [
    {
      "id": "s1",
      "title": "Write the pop() method",
      "instructions": "Inside the `Bubble` class body, write a `pop()` method that calls `this.sprite.delete()`. Click anywhere on the canvas to pop the bubble — it should disappear.",
      "hints": ["Open the shplay docs drawer on the right and find the Sprite methods section — look for how to destroy or remove a sprite from the canvas."]
    }
  ],
  "requirements": [
    {
      "id": "r1",
      "title": "pop() calls this.sprite.delete()",
      "description": "Write a `pop()` method inside `Bubble` that calls `this.sprite.delete()` to remove the wrapped sprite.",
      "type": "regex",
      "file": "script.js",
      "pattern": "pop[\\s\\S]*this\\.sprite\\.delete\\s*\\(\\s*\\)",
      "flags": "",
      "points": 0,
      "status": "pending"
    }
  ],
  "grading": { "totalPoints": 0, "passingScore": 0, "allowLateSubmit": true }
}
