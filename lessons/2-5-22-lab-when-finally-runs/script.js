// 2.5.22 When Does finally Run?

// STEP 1: In a comment, predict which blocks run when try succeeds:
//         try + catch, try + finally, or all three.

// STEP 2: Write a try block that logs "Work done." with nothing that
//         can error. Add a catch block that logs "Handling error."
//         and a finally block that logs "Cleanup ran."

// STEP 3: Run it. Confirm catch was skipped but finally still ran.
