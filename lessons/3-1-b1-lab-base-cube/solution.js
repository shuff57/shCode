// 3.1.B1 Build-Up — Base Cube — solution
let bigCube;

function setup() {
  background('#000');
  bigCube = new Cube(0, 0, 0);
  bigCube.color = 'yellow';
}

function draw() {
  background('#000');
}
