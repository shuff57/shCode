// 3.1.B1 Build-Up — Base Cube
// Start of the Spinning Sculpture project.
let bigCube;

function setup() {
  // STEP 1: Call background('#000') for a black space background.

  // STEP 2: Create bigCube = new Cube(0, 0, 0) and set its color to 'yellow'.

}

function draw() {
  background('#000');
}
