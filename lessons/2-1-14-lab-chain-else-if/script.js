// 2.1.8 Chain Three Branches

// STEP 1: Create a variable called batteryPercent and give it a number
//         from 0 to 100.

// STEP 2: Write an if / else if / else if / else chain:
//         below 20   -> log "Low battery"
//         below 50   -> log "Battery ok"
//         below 90   -> log "Battery good"
//         otherwise  -> log "Battery full"
//         Check the highest cutoff first, same as the grade chain example.
