// 2.5.20 What Happens After a throw?

// STEP 1: In a comment, predict what prints: "start", "after the throw",
//         or "caught: stop right here", and in which order.

// STEP 2: Write a try block that logs "start", then throws
//         new Error("stop right here"), then logs "after the throw".
//         In the catch block, log "caught: " + err.message.

// STEP 3: Run it. In a comment, explain why "after the throw" never
//         prints.
