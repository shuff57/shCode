// 2.1.41 Challenges: Optional Stretch

// CHALLENGE 1: Leap year checker.
//   Create `year`. Log "Leap year" when year is divisible by 4 AND
//   (NOT divisible by 100, OR divisible by 400). Log "Not a leap year"
//   otherwise. Use %, &&, ||, and !.

// CHALLENGE 2: Rock-paper-scissors winner.
//   Create playerOne and playerTwo, each "rock", "paper", or "scissors".
//   Use an else if chain to log which player wins, or "Tie" if they match.

// CHALLENGE 3: Nested shipping rule.
//   Create isMember and orderTotal.
//   If isMember: free shipping at orderTotal >= 35, otherwise a $5 fee.
//   If not a member: free shipping at orderTotal >= 75, otherwise a $10 fee.
//   Nest the orderTotal check inside the isMember check.
