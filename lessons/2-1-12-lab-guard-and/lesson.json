{
  "id": "2-1-12-lab-guard-and",
  "title": "2.1.32 Guard with &&",
  "description": "Write an if statement whose condition combines two checks with &&. Auto-graded.",
  "type": "lesson",
  "estimateMins": 8,
  "category": "Unit 1: JavaScript Fundamentals",
  "unit": "2.1 Conditionals",
  "preview": "console",
  "week": 5,
  "slos": [
    "SLO-3"
  ],
  "sourceRef": "book 2.1.8: 'Combining Conditions', Example 2.8: Both conditions must hold",
  "steps": [
    {
      "id": "s1",
      "title": "Declare two variables",
      "instructions": "Create two number variables, for example, age and score, and give each a value."
    },
    {
      "id": "s2",
      "title": "Guard with &&",
      "instructions": "Write an if statement whose condition uses && to combine two comparison checks (one for each variable). Inside the if block, log a message that says both conditions are met."
    }
  ],
  "requirements": [
    {
      "id": "r1",
      "title": "&& inside if condition",
      "description": "if statement condition uses &&",
      "type": "regex",
      "file": "script.js",
      "pattern": "if\\s*\\([^{]*&&[^{]*\\)",
      "flags": "i",
      "points": 0
    },
    {
      "id": "r2",
      "title": "two variables declared",
      "description": "Two variables are declared and given values before the if",
      "type": "regex",
      "file": "script.js",
      "pattern": "(?:const|let|var)\\s+[A-Za-z_$][\\w$]*\\s*=[\\s\\S]*?(?:const|let|var)\\s+[A-Za-z_$][\\w$]*\\s*=",
      "points": 0
    },
    {
      "id": "r3",
      "title": "a message is printed inside the if block",
      "description": "console.log runs inside the if block, as step 2 asks",
      "type": "regex",
      "file": "script.js",
      "pattern": "if\\s*\\([^{]*\\{[^}]*console\\.log",
      "points": 0
    }
  ],
  "grading": {
    "totalPoints": 0,
    "passingScore": 0,
    "allowLateSubmit": true
  }
}
