// 1.5.39 Cause an Error on Purpose

let greeting = "hello";
console.log(greeting);

// STEP 2: ReferenceError: gretting is not defined

// STEP 3: the detail (gretting is not defined) told me what to change - the
// kind (ReferenceError) and the place (line number) helped less on their own.

// STEP 4: fixed above - now prints hello.
