// 3.4.13 Write a One-Line Arrow Callback

// STEP 1: Define a function with two parameters: a value and a function.
//         Inside it, call the function you were handed, passing it the
//         value, and return whatever that call gives back. The helper
//         should not know or care what the function does.

// STEP 2: Call your helper. As the function argument, write a
//         single-expression arrow INLINE on one line: parameters, the =>
//         arrow, then the expression, with no braces and no return.

// STEP 3: Wrap the call in console.log and check the printed result
//         matches what your arrow should produce.
