// 3.2.4a Lab — Add a Cone
let cone;

function setup() {
  // STEP 1: Create cone = new Cone(0, 0, 0, 0.8, 2).
  //         The 4th argument is radius; the 5th is height.

  // STEP 2: Set cone.color to any CSS color.

}

function draw() {
  background('#111');
}
