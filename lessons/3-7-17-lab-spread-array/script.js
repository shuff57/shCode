// 3.7.17 Build a New Array with Spread

// STEP 1: Create two variables, each holding an array of at least two
//         values. Choose your own two lists.

// STEP 2: Build a new array that contains every value from both arrays,
//         using array spread. Store it in a third variable.

// STEP 3: Build a second new array that puts a loose value at the front and
//         another loose value at the back, with one of your arrays spread
//         in between.

// STEP 4: Use console.log to print both new arrays and one of the originals.
//         Check that the original still holds its starting values.
