// 3.1.11a Lab — Set colors on three Cubes and set the background.

function setup() {
  // STEP 1: Call background('#111') to set the scene background to near-black.

  // STEP 2: Create a Cube at (-2, 0, 0). Set its color to 'crimson'.

  // STEP 3: Create a Cube at (0, 0, 0). Set its color to 'gold'.

  // STEP 4: Create a Cube at (2, 0, 0). Set its color to 'deepskyblue'.

}

function draw() {
  background('#111');
}
