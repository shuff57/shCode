// 1.4.12 Name the Structure

// Each description below is a program somebody wants written.
// For each one, log which of the three structures it needs.
// Choose from: sequence, selection, repetition.

// STEP 1: add tax to a subtotal, then print the total.
//         Which structure does this need?

// STEP 2: print "pass" if the score is 60 or more, otherwise "retake".
//         Which structure does this need?

// STEP 3: print every name on a list of thirty names.
//         Which structure does this need?

// STEP 4: keep asking for a password until it is correct.
//         This one needs TWO of the three. Log both in a single
//         string, selection first — like "selection and repetition".
