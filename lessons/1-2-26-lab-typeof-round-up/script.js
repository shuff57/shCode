// 1.2.26 typeof Round-Up

// Six values. Log the type of each one directly — put the value
// itself inside typeof, not a variable holding it.
// One of the six answers is a lie. Find it.

// STEP 1: log the type of a piece of text in quotes.

// STEP 2: log the type of a number.

// STEP 3: log the type of true.

// STEP 4: log the type of undefined.

// STEP 5: log the type of null.

// STEP 6: log the type of an object in curly braces,
//         for example one with a name in it.

// Which answer was wrong, and what is that value's real type?
