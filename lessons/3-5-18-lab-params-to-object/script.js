// 3.5.18 Rewrite a Multi-Parameter Function to Take an Object

// STEP 1: Write a function that takes three positional parameters (for
//         example width, height and color) and returns a string built
//         from those three values.

// STEP 2: Rewrite the function so it takes one parameter, an options
//         object, and reads each value off that object with a dot.

// STEP 3: Call the rewritten function, passing one object literal with
//         the three keys named, and print what it returns.
