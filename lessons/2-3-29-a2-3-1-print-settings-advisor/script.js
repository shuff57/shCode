// 2.3.30 A2.3.1: Print Settings Advisor

// STEP 1: Create a variable called filamentType.
//         Set it to one of "PLA", "ABS", "PETG", "TPU".

// STEP 2: Write a switch on filamentType with a case for each of the
//         four filament types above. Each case must print a
//         recommended temperature AND a recommended print speed for
//         that filament: pick reasonable numbers. Add a default for
//         a filament type you do not recognize. End every branch the
//         way the readings showed.

// STEP 3: Above one of your case labels, write a comment explaining
//         exactly what would print for that filament, and for which
//         cases below it, if its stopping statement were removed.
//         Do not actually remove it; the comment is the deliverable.
