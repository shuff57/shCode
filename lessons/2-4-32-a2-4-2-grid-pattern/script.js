// 2.4.32 A2.4.2 — Print a Grid Pattern

// STEP 1: Declare rows and cols as variables, each set to at least 5.
//         The loops below must read these variables — do not write
//         the numbers directly into the loop headers.

// STEP 2: Write an outer for loop with counter row that runs while
//         row is less than rows. Inside it, declare an empty string
//         called line. Write an inner for loop with counter col that
//         runs while col is less than cols, appending a character
//         (like "#" or "*") to line on every pass.

// STEP 3: After the inner loop closes, log line.
//         Try changing rows and cols (keeping both at least 5) and
//         confirm the grid resizes without touching the loops.
