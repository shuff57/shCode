// 3.5.22 Destructure 3 Fields From an Object

// STEP 1: Create an object with at least three properties (a title, a
//         cost, a quantity, whatever you like).

// STEP 2: Unpack three of those properties into variables in one statement.
//         Braces on the left of the equals sign, names matching the keys.

// STEP 3: Build a sentence from the three bare variable names (no dot, no
//         object name) and print it.

// STEP 4: Write a function that takes one options object and destructures
//         two or more of its keys right in the parameter list, then returns
//         a string built from the bare names.
