// 3.1.7d Lab — Scale a Cube non-uniformly.
let cube;

function setup() {
  background('#111');
  // STEP 1: Create a Cube at (0, 0, 0) and store it in cube.

  // STEP 2: Set cube.scale.x to 2 (twice as wide).

  // STEP 3: Set cube.scale.y to 0.5 (half as tall).

}

function draw() {
  background('#111');
}
