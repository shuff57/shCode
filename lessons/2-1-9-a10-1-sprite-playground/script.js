// 2.1.10 Sprite Playground — combine canvas, WASD movement, auto-motion, and text.

let player, mover;

function setup() {
  // STEP 1: Create the canvas (any size from 300×300 to 800×600), then create
  // two sprites — `player` (you'll control it with WASD) and `mover` (will
  // animate on its own).

}

function draw() {
  // STEP 5: Clear the background each frame so old frames don't stack up.

  // STEP 2: Wire WASD to the player — read kb.pressing for 'a' / 'd' / 'w' / 's'
  // and set player.vel.x / player.vel.y. Include an else branch that resets
  // velocity to 0 so the player stops when no key is held.

  // STEP 3: Animate the mover sprite using frameCount. Pick any pattern —
  // oscillate, orbit, or spin.

  // STEP 4: Draw an on-screen text label using text() and textSize(). Set fill()
  // before text() so the label is visible on the background.

}
