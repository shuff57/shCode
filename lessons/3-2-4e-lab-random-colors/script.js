// 3.2.4e Lab — random() colors
let spheres = [];

function setup() {
  // STEP 1: Use a for loop to create 5 Spheres.
  //         Place each at random(-5, 5) for x and z, fixed y = 0, radius 0.5.
  //         Push each sphere into the spheres array.

  // STEP 2: Inside the loop, give each sphere a random RGB color.
  //         Use Math.floor(random(50, 255)) for each channel.

}

function draw() {
  background('#111');
}
