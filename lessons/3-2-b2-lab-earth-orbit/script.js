// 3.2.B2 Build-Up — Earth Orbits
// Starter includes B1's solution. Add only what B2 asks for.
let sun, earth;

function setup() {
  background('#000');
  sun = new Sphere(0, 0, 0, 1.2);
  sun.color = 'yellow';

  // STEP 1: Create earth = new Sphere(4, 0, 0, 0.6) and set its color to 'dodgerblue'.

}

function draw() {
  background('#000');

  // STEP 2: Set earth.position.x = Math.cos(radians(frameCount * 0.6)) * 4
  // STEP 3: Set earth.position.z = Math.sin(radians(frameCount * 0.6)) * 4

}
