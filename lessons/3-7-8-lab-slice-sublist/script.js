// 3.7.8 Extract a Sub-List with .slice()

// STEP 1: Create a variable called items and assign it an array of at least
//         five values. Choose your own list, for example a list of names.

// STEP 2: Use .slice(start, end) to copy a middle part of items into a new
//         variable. The end index is NOT included in the result.

// STEP 3: Use .slice(start) with only a start index to copy everything from
//         a position to the end of the array into a second variable.

// STEP 4: Use console.log to print both slices and the original items array.
//         Check that the original still holds every value you started with.
