// 2.5.24 A16.1 Save System — Build save/load with 3+ slots

function setup() {
  // STEP 1: Create a canvas, a player, and a way to earn points

}

function draw() {
  background('#222');

  // STEP 2: Player movement (keyboard or mouse)

  // STEP 3: Save to a slot (pick a key, build a save object, stringify, storeItem)

  // STEP 4: Load from a slot (getItem, parse, apply to player + score)

  // STEP 5: Auto-save when the player reaches a goal (score threshold or position)

}
