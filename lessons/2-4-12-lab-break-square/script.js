// 2.4.12 Break When the Square Passes 50

// STEP 1: Declare a variable called n and set it to 1.

// STEP 2: Write a loop whose condition never turns false on its own —
//         the "true" condition from lesson 2.4.10 — so the only exit
//         is the one you write inside it.
//         Inside, check whether n * n is greater than 50.
//         If it is, log n and its square, then leave the loop early
//         using the keyword from lesson 2.4.9.
//         Otherwise, add 1 to n and let the loop go around again.
