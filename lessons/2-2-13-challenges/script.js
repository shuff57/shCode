// 2.2.12 Challenges — Optional Stretch. Pick one and go.

function setup() {
  new Canvas(400, 400);
  // STEP 1: define at least one class (Enemy with isAlive, Player, or PowerUp extends Collectible)

  // STEP 2: instantiate your class — call new <ClassName>(...) at least once
}

function draw() {
  background('#222');
  // STEP 3: call a method on your instance (e.g., enemy.isAlive(), player.move('left'))
}
