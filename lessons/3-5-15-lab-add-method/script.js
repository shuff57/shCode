// 3.5.15 Add a Method to an Object

// STEP 1: Create a variable holding an object literal with at least one
//         data property (a name, a count, anything).

// STEP 2: Give the object a property whose value is a function. Name the
//         property clearly, and have the function hand a value back.

// STEP 3: Call that method by writing its name followed by parentheses,
//         and print what it returns.
