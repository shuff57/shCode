// 3.2.4b Lab — Add a Cylinder
let cylinder;

function setup() {
  // STEP 1: Create cylinder = new Cylinder(0, 0, 0, 0.5, 3).
  //         The 4th argument is radius; the 5th is height.

  // STEP 2: Set cylinder.color to any CSS color.

}

function draw() {
  background('#111');
}
