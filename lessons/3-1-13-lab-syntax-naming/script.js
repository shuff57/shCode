// 3.1.5 Fix the Syntax and the Name

// This function has two problems: a missing piece in the header, and a
// name that breaks convention.

function ShowTotal {
  console.log("Total: 42")
}

// STEP 1: Fix the header — every function header needs an empty pair of
//         parentheses after the name, even when the function takes nothing.

// STEP 2: Rename the function to camelCase: showTotal.

// STEP 3: Call showTotal() below so the body actually runs.
