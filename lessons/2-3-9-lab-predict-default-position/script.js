// 2.3.8 Predict Where default Runs

// STEP 1: Create a variable called grade set to "A", "B", or anything else.
//         Write a comment predicting which line will print.
//         Then write a switch on grade: a case for "A", a case for "B",
//         and a default, each branch ending the way the readings showed.

// STEP 2: Create a second variable, for example n, set to a number that
//         matches none of your cases. Write a second switch where the
//         default label is written BEFORE the last case label instead
//         of after it. Predict in a comment whether default still runs.
//         Then run the file and compare.
