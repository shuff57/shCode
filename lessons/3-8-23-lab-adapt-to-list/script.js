// 3.8.23 Adapt the Pattern to a List

// STEP 1: Create a variable holding an array of at least two objects. Each
//         object should describe one thing with a name and a value.

// STEP 2: Use JSON.stringify on the array and store it with
//         localStorage.setItem under a key.

// STEP 3: Read the key with localStorage.getItem, then parse it back into an
//         array using JSON.parse. An array round-trips exactly like an object.

// STEP 4: Use console.log to print the restored array's length and one
//         record's field, confirming the array and its objects came back whole.
