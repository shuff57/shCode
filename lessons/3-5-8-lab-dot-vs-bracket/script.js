// 3.5.8 Dot vs. Bracket

// STEP 1: Create an object that has at least one property whose name
//         contains a space. Write that key in quotes.

// STEP 2: Print the value of the spaced-out property. A dot cannot reach
//         a key with a space in it, so use square brackets with the quoted key.

// STEP 3: Create a variable that holds the name of one of the object's
//         properties, as a string.

// STEP 4: Print that property's value using the variable inside square brackets.

// STEP 5: Use Object.keys with a loop to print every key and its value.
//         Inside the loop the name lives in a variable, so each value has
//         to be fetched with brackets too.
