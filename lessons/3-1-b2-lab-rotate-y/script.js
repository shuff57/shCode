// 3.1.B2 Build-Up — Rotate on Y
// Starter includes B1's solution. Add only what B2 asks for.
let bigCube;

function setup() {
  background('#000');
  bigCube = new Cube(0, 0, 0);
  bigCube.color = 'yellow';
}

function draw() {
  background('#000');
  // STEP 1: In draw(), assign bigCube.rotation.y using radians() and frameCount.
  //         Use a multiplier of 1.5 (so the cube spins ~1.5 degrees per frame).

}
