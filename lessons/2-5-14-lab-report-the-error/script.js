// 2.5.14 Report the Error

// STEP 1: Inside a try block, declare a constant set to null, then read
//         a property off it (for example .name). Reading a property of
//         null always raises a TypeError.

// STEP 2: In the catch block, log err.name, then log err.message.
