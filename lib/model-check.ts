// Moved to reshape-cad's packages/script (B1 extraction, plan:
// freecad-browser.md). The real source lives at
// reshape-cad/packages/script/src/model-check.ts; this file is a thin
// re-export so shCode's existing 'lib/model-check' import paths
// (grader.ts, lib/types.ts) do not have to change.
export * from '@shuff57/reshape-script/model-check';
