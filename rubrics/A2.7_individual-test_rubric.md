# Rubric — A2.7 Chapter 2 Individual Performance Assessment

**Quarter 1 · Week 8 · Wed Sep 30, 2026 · Individual · ~53 minutes**

**Weights:** Part A (Concepts) 25% · Part B (Trace) 25% · Part C (Find & Fix) 20% · Part D (Design+Write) 30%

---

## Part A: Concepts (2.7.1) — 25 pts

| Item | Points | Description |
|---|---:|---|
| MCQ 1 — `if (x == true)` redundancy | 5 | Answer: "redundant if x is already boolean" |
| MCQ 2 — Paradigm for decisions | 5 | Answer: procedural/structured |
| MCQ 3 — Flowchart shapes for loop+decision | 5 | Answer: oval, rectangle, diamond, hexagon, parallelogram (5 shapes) |

**Part A1: MCQs — 15 pts**

| Trace shape | Points | Example |
|---|---:|---|
| `==` truthiness trap | 5 | `0 == false` → true |
| `for` with step ≠ 1 | 5 | `for (let i=2; i<=10; i+=2)` |
| `switch` fall-through | 5 | Missing `break` on case 2 |
| Nested loop trace | 5 | `for (row) { for (col) ... }` |
| `try/catch/finally` print order | 5 | A, catch, finally, after |

**Part B: Traces — 25 pts**

**Part A2: Short answers (2.7.2) — 10 pts**

| Item | Points | Rubric |
|---|---:|---|
| `for` vs `while` | 4 | `for` = known count/range; `while` = condition-driven |
| `===` vs `==` | 3 | `===` checks value+type; `==` coerces (e.g., `0==false` true) |
| `throw` vs `console.log` | 3 | `throw` jumps to `catch`/crashes; `log` just prints |

---

## Part C: Find and Fix (2.7.3) — 20 pts

| Bug | Points | Bug kind | Required fix |
|---|---:|---|---|
| 1. Unterminated string | 5 | syntax | Close the quote on `"Notebook` |
| 2. `beforTax` typo | 5 | runtime | Fix to `beforeTax` |
| 3. Loop accumulator wrong | 5 | logic | Move accumulation before `continue` |
| 4. `continue` skips increment in `while` | 5 | runtime | Move `i++` before `continue` |

**Each bug: 3 pts for correct fix, 2 pts for correct kind name (syntax/runtime/logic)**

---

## Part D1: Chart It (2.7.4) — 10 pts

| Criterion | Points |
|---|---:|
| Legal flowchart (all 8 structural checks) | 4 |
| Uses hexagon for loop setup | 3 |
| Decision diamond with two labeled exits | 2 |
| At least two rectangles + parallelogram for I/O | 2 |

**Blank D1 = 0/10** (even if D2 is perfect).

---

## Part D2: Write It (2.7.5) — 20 pts

| Criterion | Points | What earns full credit |
|---|---:|---|
| Nested loop | 8 | Two nested `for`/`while` loops; both execute |
| Branch with `&&`/`\|\|` | 8 | `if`/`else if`/`else` with `&&` or `\|\|` in condition |
| `break` or `continue` | 4 | One `break`/`continue` inside nested loop, affects flow |

**Partial credit ladder:**
- 20: All three criteria met, runs correctly
- 16: One minor issue (off-by-one bounds, missing `typeof`)
- 12: Two of three structural elements
- 8: Only one structural element
- 4: Significant logic error but some structure
- 0: Blank or fundamentally wrong

**D2 graded against the problem, not against D1.** A correct chart with buggy code loses only D2 points. A correct chart with buggy code loses only D2 points. Blank D1 costs the 10.

---

## Grading Notes

- **All parts are summative** (`summative: true`). One attempt, no marking shown, no explanations, score hidden. Completion = submitting.
- **Part C (2.7.3) and Part D1 (2.7.4) allow unlimited attempts** — repairing and redrawing ARE the skills. Student sees only "which symptom remains."
- **Part D2 (2.7.5) is one attempt, no Run button.** Code must be right on paper.
- **`check-summative-parts.mjs`** must pass before the day (enforces `summative: true` on all five parts).
- **No retakes.** Score on Sep 30 is final. Absence = teacher judgement.
- **Aeries category:** **Individual Assessments (50%)** per syllabus. 1.7's "15% Individual Chapter Tests" line is wrong — fix it to match syllabus.

---

## Study Guide Policy

- **Sep 28 (PA day):** Hand out the printed **rubric** as the study guide. Every item type on the test has an analogue in the PA.
- **Sep 30 (Test day):** Hand back the **graded PA** at the start of the period. The rubric is the study guide; the PA is the rehearsal.
- **Does the PA leak the test?** 2.7's D1/D2 menu uses **different problems** from 2.6's menu (Locker Sweep / Word Audit / Savings Run / Coin Drawer). The study guide is safe.